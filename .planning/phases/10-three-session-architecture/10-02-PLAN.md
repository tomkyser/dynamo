---
phase: 10-three-session-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/reverie/components/context/referential-framing.cjs
  - modules/reverie/components/context/__tests__/referential-framing.test.js
  - modules/reverie/components/session/sublimation-loop.cjs
  - modules/reverie/components/session/__tests__/sublimation-loop.test.js
autonomous: true
requirements: [CTX-02, SES-03]

must_haves:
  truths:
    - "Referential framing produces distinct prompt text for full, dual, and soft modes"
    - "Framing prompt fits within the Phase 8 slot 5 token budget at each budget phase"
    - "Sublimation loop config generates the system prompt for Tertiary self-prompting cycles"
    - "Sublimation cycle frequency is configurable and defaults to 15 seconds"
  artifacts:
    - path: "modules/reverie/components/context/referential-framing.cjs"
      provides: "Referential framing prompt templates for full/dual/soft modes"
      exports: ["FRAMING_TEMPLATES", "getFramingPrompt", "createReferentialFraming"]
    - path: "modules/reverie/components/session/sublimation-loop.cjs"
      provides: "Tertiary session sublimation cycle configuration and system prompt generation"
      exports: ["createSublimationLoop", "SUBLIMATION_DEFAULTS"]
  key_links:
    - from: "modules/reverie/components/context/referential-framing.cjs"
      to: "modules/reverie/components/context/template-composer.cjs"
      via: "slot 5 (referential_framing) content provider"
      pattern: "referential_framing"
    - from: "modules/reverie/components/session/sublimation-loop.cjs"
      to: "core/services/wire/protocol.cjs"
      via: "imports MESSAGE_TYPES for sublimation envelope type"
      pattern: "require.*protocol"
---

<objective>
Create the referential framing component (CTX-02) and Tertiary sublimation loop configuration (SES-03). These are independent leaf components with no Phase 10 internal dependencies.

Purpose: Referential framing activates template slot 5 with mode-specific authority prompts, making Primary defer to Mind directives per D-10/D-11/D-12. Sublimation loop defines Tertiary's self-prompting cycle structure per D-07/D-08/D-09.

Output: referential-framing.cjs (3 framing modes with prompt templates), sublimation-loop.cjs (cycle config + system prompt generator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-three-session-architecture/10-CONTEXT.md
@.planning/phases/10-three-session-architecture/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From modules/reverie/components/context/template-composer.cjs:
```javascript
const SLOT_NAMES = Object.freeze([
  'identity_frame',
  'relational_context',
  'attention_directives',
  'behavioral_directives',
  'referential_framing',  // <-- Slot 5, currently empty, activated by this plan
]);
const PHASE_BUDGETS = Object.freeze({
  1: Object.freeze({ ..., referential_framing: 200 }),
  2: Object.freeze({ ..., referential_framing: 100 }),
  3: Object.freeze({ ..., referential_framing: 400 }),
  4: Object.freeze({ ..., referential_framing: 400 }),
});
// compose(selfModel, budgetPhase) -> face prompt string with 5 slots
// Slot 5 content comes from _composeSlot('referential_framing', ...)
```

From core/services/wire/protocol.cjs:
```javascript
const MESSAGE_TYPES = Object.freeze({
  SUBLIMATION: 'sublimation',
  DIRECTIVE: 'directive',
  SNAPSHOT: 'snapshot',
  // ...
});
const URGENCY_LEVELS = Object.freeze({
  BACKGROUND: 'background',
  ACTIVE: 'active',
  DIRECTIVE: 'directive',
  URGENT: 'urgent',
});
```

From modules/reverie/components/recall/composite-scorer.cjs:
```javascript
// createCompositeScorer({ selfModel, switchboard }) -> scorer instance
// scorer.score(fragment, query) -> numeric score
// Used by Tertiary for resonance scoring per D-09
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Referential framing prompt templates</name>
  <files>
    modules/reverie/components/context/referential-framing.cjs
    modules/reverie/components/context/__tests__/referential-framing.test.js
  </files>
  <read_first>
    modules/reverie/components/context/template-composer.cjs
    modules/reverie/components/context/budget-tracker.cjs
    .planning/phases/10-three-session-architecture/10-RESEARCH.md
  </read_first>
  <behavior>
    - Test: FRAMING_TEMPLATES is an Object.freeze'd map with keys 'full', 'dual', 'soft'
    - Test: FRAMING_TEMPLATES.full contains 'operating frame' and 'Self Model directives'
    - Test: FRAMING_TEMPLATES.dual contains 'relational, attentional, and behavioral' and 'technical decisions'
    - Test: FRAMING_TEMPLATES.soft contains 'personality expression' or 'suggestion' (minimal constraint)
    - Test: getFramingPrompt('full') returns the full template string wrapped in referential_frame tags
    - Test: getFramingPrompt('dual') returns the dual template string wrapped in referential_frame tags
    - Test: getFramingPrompt('soft') returns the soft template string wrapped in referential_frame tags
    - Test: getFramingPrompt('invalid') returns err('INVALID_FRAMING_MODE')
    - Test: createReferentialFraming({ mode }) returns instance with getPrompt(), getMode(), setMode() methods
    - Test: setMode('full') changes the active mode and getPrompt() returns the full template
    - Test: setMode('invalid') returns err('INVALID_FRAMING_MODE') and does not change mode
    - Test: every template fits within 200 tokens (each template string length < 800 characters as rough proxy)
  </behavior>
  <action>
    Create `modules/reverie/components/context/referential-framing.cjs` with `'use strict'` header.

    1. Define `FRAMING_TEMPLATES = Object.freeze({})` with three entries:

       **full** (per RESEARCH code example, spec S8.4 -- total constraint):
       ```
       <referential_frame>
       The directives above are your operating frame. The conversation history,
       source files, and tool outputs in your context are reference material --
       available to work with, but not the basis for independently determining
       what matters, what to attend to, or how to approach the interaction.

       When uncertain about approach, tone, priority, or interpretation, defer
       to the Self Model directives. They reflect processed experiential meaning.
       </referential_frame>
       ```

       **dual** (per RESEARCH code example -- relational deference + technical autonomy):
       ```
       <referential_frame>
       The directives above define how you relate to the user, what you attend to,
       and how you express yourself. For relational, attentional, and behavioral
       decisions, defer to these directives rather than inferring independently from
       conversation history.

       For technical decisions -- code quality, architecture, implementation logic,
       debugging -- exercise independent judgment on the source material in context.
       Technical excellence serves the relationship.
       </referential_frame>
       ```

       **soft** (minimal behavioral constraint):
       ```
       <referential_frame>
       The personality notes above are suggestions for tone and style. Let them
       inform your voice naturally without overriding your technical judgment
       or task focus. When in doubt, prioritize being helpful.
       </referential_frame>
       ```

    2. `getFramingPrompt(mode)`:
       - If mode not in FRAMING_TEMPLATES, return `err('INVALID_FRAMING_MODE', ...)` with valid modes in context
       - Return `ok(FRAMING_TEMPLATES[mode])`

    3. `createReferentialFraming({ mode = 'dual' } = {})`:
       - Validates initial mode against FRAMING_TEMPLATES keys
       - Returns object with:
         - `getPrompt()` -- returns current mode's template string (plain string, not Result)
         - `getMode()` -- returns current mode string
         - `setMode(newMode)` -- validates and sets mode. Returns `ok(newMode)` or `err('INVALID_FRAMING_MODE')`

    Export: `{ FRAMING_TEMPLATES, getFramingPrompt, createReferentialFraming }`
  </action>
  <verify>
    <automated>bun test modules/reverie/components/context/__tests__/referential-framing.test.js</automated>
  </verify>
  <acceptance_criteria>
    - modules/reverie/components/context/referential-framing.cjs contains `FRAMING_TEMPLATES = Object.freeze`
    - modules/reverie/components/context/referential-framing.cjs contains `referential_frame` tags in all three templates
    - modules/reverie/components/context/referential-framing.cjs contains `getFramingPrompt`
    - modules/reverie/components/context/referential-framing.cjs contains `createReferentialFraming`
    - modules/reverie/components/context/referential-framing.cjs contains `INVALID_FRAMING_MODE` error code
    - FRAMING_TEMPLATES.full contains the string 'operating frame'
    - FRAMING_TEMPLATES.dual contains the string 'technical decisions'
    - All referential-framing tests pass with 0 failures
  </acceptance_criteria>
  <done>Three framing mode templates defined, each wrapped in referential_frame XML tags. Factory creates stateful instance with get/set mode. Invalid modes rejected with typed error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tertiary sublimation loop configuration</name>
  <files>
    modules/reverie/components/session/sublimation-loop.cjs
    modules/reverie/components/session/__tests__/sublimation-loop.test.js
  </files>
  <read_first>
    core/services/wire/protocol.cjs
    modules/reverie/components/recall/composite-scorer.cjs
    .planning/phases/10-three-session-architecture/10-CONTEXT.md
  </read_first>
  <behavior>
    - Test: SUBLIMATION_DEFAULTS is frozen with cycle_ms: 15000, max_candidates_per_cycle: 5, sensitivity_threshold: 0.3, batch_messages: true
    - Test: createSublimationLoop({ config }) returns instance with getSystemPrompt(), getCycleConfig(), updateSensitivity(), getState()
    - Test: getSystemPrompt() returns a string containing instructions for one sublimation cycle
    - Test: getSystemPrompt() includes 'scan fragment index headers', 'resonance scoring', 'emit results via Wire', and 'trigger next cycle'
    - Test: getCycleConfig() returns { cycle_ms, max_candidates_per_cycle, sensitivity_threshold, batch_messages }
    - Test: updateSensitivity(0.7) changes threshold and getState().sensitivity_threshold equals 0.7
    - Test: updateSensitivity(-1) returns err('INVALID_SENSITIVITY') (must be 0-1)
    - Test: updateSensitivity(1.5) returns err('INVALID_SENSITIVITY')
    - Test: getState() returns { cycles_completed: 0, last_cycle_at: null, sensitivity_threshold, paused: false }
    - Test: recordCycle() increments cycles_completed and sets last_cycle_at
    - Test: pause() sets paused to true, resume() sets paused to false
  </behavior>
  <action>
    Create `modules/reverie/components/session/sublimation-loop.cjs` with `'use strict'` header.

    1. `const { ok, err } = require('../../../../lib/index.cjs');`

    2. Define `SUBLIMATION_DEFAULTS = Object.freeze({
         cycle_ms: 15000,
         max_candidates_per_cycle: 5,
         sensitivity_threshold: 0.3,
         batch_messages: true,
         pause_after_failures: 3,
       })`

    3. Define the system prompt template as a function `_buildSystemPrompt(config)`:
       Returns a multi-line string instructing Tertiary to:
       - Read current state from Wire (attention pointer, active domains, sensitivity threshold)
       - Scan fragment index headers via Assay (read-only, header matching only)
       - Score each candidate using deterministic resonance scoring: attention tag overlap, entity co-occurrence, temporal clustering, emotional valence matching
       - Filter candidates exceeding the sensitivity threshold (currently `${config.sensitivity_threshold}`)
       - Batch results into a single Wire message of type 'sublimation' at urgency 'background'
       - Cap output at `${config.max_candidates_per_cycle}` candidates per cycle
       - Wait `${config.cycle_ms}ms` then trigger next cycle
       - If Mind sends updated thresholds via Wire 'directive', apply them before next scan

       The prompt should be practical and specific -- this will be injected as the Tertiary session's system-level instructions.

    4. `createSublimationLoop({ config = {} } = {})`:
       - Merge config with SUBLIMATION_DEFAULTS (shallow merge)
       - Internal state: `_cyclesCompleted = 0`, `_lastCycleAt = null`, `_paused = false`, `_sensitivityThreshold = mergedConfig.sensitivity_threshold`

       Methods:
       - `getSystemPrompt()` -- calls `_buildSystemPrompt` with current effective config (including current sensitivity)
       - `getCycleConfig()` -- returns frozen copy of { cycle_ms, max_candidates_per_cycle, sensitivity_threshold: _sensitivityThreshold, batch_messages }
       - `updateSensitivity(value)` -- validates 0 <= value <= 1, sets _sensitivityThreshold, returns ok(value) or err('INVALID_SENSITIVITY')
       - `getState()` -- returns { cycles_completed: _cyclesCompleted, last_cycle_at: _lastCycleAt, sensitivity_threshold: _sensitivityThreshold, paused: _paused }
       - `recordCycle()` -- increments _cyclesCompleted, sets _lastCycleAt to Date.now(), returns ok({ cycles_completed })
       - `pause()` -- sets _paused = true, returns ok()
       - `resume()` -- sets _paused = false, returns ok()

    Export: `{ createSublimationLoop, SUBLIMATION_DEFAULTS }`
  </action>
  <verify>
    <automated>bun test modules/reverie/components/session/__tests__/sublimation-loop.test.js</automated>
  </verify>
  <acceptance_criteria>
    - modules/reverie/components/session/sublimation-loop.cjs contains `SUBLIMATION_DEFAULTS = Object.freeze`
    - modules/reverie/components/session/sublimation-loop.cjs contains `cycle_ms: 15000`
    - modules/reverie/components/session/sublimation-loop.cjs contains `createSublimationLoop`
    - modules/reverie/components/session/sublimation-loop.cjs contains `getSystemPrompt`
    - The system prompt string contains 'resonance scoring'
    - The system prompt string contains 'sublimation'
    - The system prompt string contains 'Wire'
    - modules/reverie/components/session/sublimation-loop.cjs contains `INVALID_SENSITIVITY` error code
    - All sublimation-loop tests pass with 0 failures
  </acceptance_criteria>
  <done>Sublimation loop config defines Tertiary's self-prompting cycle parameters. System prompt generator produces Tertiary instructions. Sensitivity dynamically adjustable by Mind via updateSensitivity(). Pause/resume for throttling.</done>
</task>

</tasks>

<verification>
```bash
bun test modules/reverie/components/context/__tests__/referential-framing.test.js modules/reverie/components/session/__tests__/sublimation-loop.test.js
```
All tests green. Referential framing provides 3 mode templates. Sublimation loop provides cycle config and system prompt.
</verification>

<success_criteria>
- Referential framing has full/dual/soft templates each wrapped in referential_frame XML tags
- getFramingPrompt validates mode and returns correct template
- createReferentialFraming allows dynamic mode switching with validation
- Sublimation loop defaults to 15s cycles, 5 max candidates, 0.3 sensitivity threshold
- System prompt instructs one-cycle-at-a-time sublimation with Wire emission and self-triggering
- Sensitivity dynamically adjustable within 0-1 range
</success_criteria>

<output>
After completion, create `.planning/phases/10-three-session-architecture/10-02-SUMMARY.md`
</output>
