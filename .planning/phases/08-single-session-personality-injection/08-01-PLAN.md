---
phase: 08-single-session-personality-injection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/reverie/components/context/budget-tracker.cjs
  - modules/reverie/components/context/__tests__/budget-tracker.test.js
  - modules/reverie/components/context/template-composer.cjs
  - modules/reverie/components/context/__tests__/template-composer.test.js
autonomous: true
requirements: [CTX-01, CTX-03]

must_haves:
  truths:
    - "Budget tracker maps cumulative byte counts to 4 budget phases at research-backed thresholds (30/60/80%)"
    - "Budget tracker transitions are deterministic given the same byte count and context window size"
    - "Template composer fills 5 named slots from Self Model aspect data"
    - "Template composer produces injection text sized differently for each budget phase (1200/800/1900/1800 token targets)"
    - "Template composer reads Self Model aspects via getAspect() synchronous cache"
  artifacts:
    - path: "modules/reverie/components/context/budget-tracker.cjs"
      provides: "Pure state machine for budget phase calculation"
      exports: ["BUDGET_PHASES", "PHASE_THRESHOLDS", "DEFAULT_CONTEXT_WINDOW_TOKENS", "calculateBudgetPhase", "createBudgetTracker"]
    - path: "modules/reverie/components/context/template-composer.cjs"
      provides: "5-slot face prompt template composition from Self Model data"
      exports: ["SLOT_NAMES", "PHASE_BUDGETS", "createTemplateComposer"]
    - path: "modules/reverie/components/context/__tests__/budget-tracker.test.js"
      provides: "Tests for budget phase transitions at all 4 thresholds"
    - path: "modules/reverie/components/context/__tests__/template-composer.test.js"
      provides: "Tests for template composition across all 4 budget phases"
  key_links:
    - from: "modules/reverie/components/context/template-composer.cjs"
      to: "modules/reverie/components/self-model/self-model.cjs"
      via: "selfModel.getAspect() synchronous Magnet cache read"
      pattern: "selfModel\\.getAspect\\("
    - from: "modules/reverie/components/context/template-composer.cjs"
      to: "modules/reverie/components/context/budget-tracker.cjs"
      via: "PHASE_BUDGETS keyed by budget phase number"
      pattern: "PHASE_BUDGETS\\["
---

<objective>
Build the two pure-computation sub-components of the Context Manager: the budget tracker state machine (CTX-03) and the template composer (CTX-01). These are stateless/deterministic modules that the Context Manager orchestrator (Plan 02) will consume.

Purpose: Establish the foundational calculations for context budget management and face prompt composition before wiring them into the stateful Context Manager. Both components are independently testable with no I/O dependencies.

Output: Two CJS modules with full test coverage -- budget-tracker.cjs (phase calculation) and template-composer.cjs (5-slot template fill from Self Model data).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-single-session-personality-injection/08-CONTEXT.md
@.planning/phases/08-single-session-personality-injection/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/result.cjs:
```javascript
function ok(value) { return { ok: true, value }; }
function err(code, message, context) { return { ok: false, error: { code, message, context } }; }
```

From lib/contract.cjs:
```javascript
function createContract(name, shape, implementation) {
  // Validates required methods, returns ok(Object.freeze({...implementation}))
}
```

From modules/reverie/lib/constants.cjs:
```javascript
const SM_ASPECTS = Object.freeze(['identity-core', 'relational-model', 'conditioning']);
const DATA_DIR_DEFAULT = '~/.dynamo/reverie';
```

From modules/reverie/components/self-model/self-model.cjs:
```javascript
// getAspect(aspectName) returns cached data or null (synchronous Magnet read)
// Returns: { frontmatter: { personality_traits: {...}, communication_style: {...}, ... }, body: '...' }
const SELF_MODEL_SHAPE = { required: ['save', 'load', 'getAspect', 'setAspect', 'getVersion'], optional: [] };
```

From modules/reverie/components/self-model/cold-start.cjs:
```javascript
// createColdStartSeed returns:
// { identityCore: { frontmatter: { personality_traits, communication_style, value_orientations, expertise_map, boundaries }, body },
//   relationalModel: { frontmatter: { trust_calibration, interaction_rhythm, ... }, body },
//   conditioning: { frontmatter: { attention_biases, association_priors, sublimation_sensitivity, recall_strategies, error_history }, body } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Budget Tracker State Machine</name>
  <files>modules/reverie/components/context/budget-tracker.cjs, modules/reverie/components/context/__tests__/budget-tracker.test.js</files>
  <read_first>
    - modules/reverie/components/context/budget-tracker.cjs (will be new -- .gitkeep exists in parent)
    - modules/reverie/lib/constants.cjs (for DATA_DIR_DEFAULT pattern and Object.freeze convention)
    - lib/result.cjs (ok/err pattern)
    - lib/contract.cjs (createContract pattern)
    - .planning/phases/08-single-session-personality-injection/08-CONTEXT.md (D-05 through D-07)
    - .planning/phases/08-single-session-personality-injection/08-RESEARCH.md (Pattern 2: Budget Phase State Machine)
  </read_first>
  <behavior>
    - calculateBudgetPhase(0, 800000) returns BUDGET_PHASES.FULL (1)
    - calculateBudgetPhase(240000, 800000) returns BUDGET_PHASES.COMPRESSED (2) -- exactly 30%
    - calculateBudgetPhase(239999, 800000) returns BUDGET_PHASES.FULL (1) -- just below 30%
    - calculateBudgetPhase(480000, 800000) returns BUDGET_PHASES.REINFORCED (3) -- exactly 60%
    - calculateBudgetPhase(640000, 800000) returns BUDGET_PHASES.COMPACTION (4) -- exactly 80%
    - calculateBudgetPhase(800000, 800000) returns BUDGET_PHASES.COMPACTION (4) -- 100%
    - createBudgetTracker({contextWindowTokens: 200000}).getPhase() returns 1 initially
    - tracker.trackBytes(250000) transitions to phase 2 and returns { changed: true, from: 1, to: 2 }
    - tracker.trackBytes(100) with no transition returns { changed: false, from: 2, to: 2 }
    - tracker.reset(132000) sets bytes to 132000 and recalculates phase (for post-compaction reset)
    - tracker.getUtilization() returns cumulativeBytes / contextWindowBytes as a ratio
    - tracker.getStats() returns { cumulativeBytes, contextWindowBytes, phase, utilization, turnCount }
    - tracker.incrementTurn() increases turn count
  </behavior>
  <action>
    Create `modules/reverie/components/context/budget-tracker.cjs` implementing the budget phase state machine per D-05, D-06, D-07.

    **Constants (all Object.freeze):**
    - `BUDGET_PHASES`: `{ FULL: 1, COMPRESSED: 2, REINFORCED: 3, COMPACTION: 4 }`
    - `PHASE_THRESHOLDS`: `{ COMPRESSED_AT: 0.30, REINFORCED_AT: 0.60, COMPACTION_AT: 0.80 }` -- per D-06 research-backed thresholds, NOT spec thresholds
    - `DEFAULT_CONTEXT_WINDOW_TOKENS`: `200000` -- configurable default (200K tokens)
    - `BYTES_PER_TOKEN`: `4` -- heuristic per D-07 (~4 bytes/token for English)
    - `DEFAULT_POST_COMPACTION_TOKENS`: `33000` -- ~33K tokens post-compaction buffer

    **Pure function:**
    - `calculateBudgetPhase(cumulativeBytes, contextWindowBytes)` -- returns phase number (1-4). Thresholds: >= 0.80 -> 4, >= 0.60 -> 3, >= 0.30 -> 2, else -> 1.

    **Factory function:**
    - `createBudgetTracker(options)` -- options: `{ contextWindowTokens }` (default 200000)
    - Internal state: `_cumulativeBytes = 0`, `_phase = 1`, `_turnCount = 0`, `_contextWindowBytes = contextWindowTokens * BYTES_PER_TOKEN`
    - Methods:
      - `trackBytes(byteCount, source)` -- adds bytes, recalculates phase, returns `{ changed: boolean, from: number, to: number }`
      - `getPhase()` -- returns current phase number (1-4)
      - `getUtilization()` -- returns `_cumulativeBytes / _contextWindowBytes`
      - `getStats()` -- returns `{ cumulativeBytes: _cumulativeBytes, contextWindowBytes: _contextWindowBytes, phase: _phase, utilization, turnCount: _turnCount }`
      - `reset(postCompactionBytes)` -- sets `_cumulativeBytes = postCompactionBytes || DEFAULT_POST_COMPACTION_TOKENS * BYTES_PER_TOKEN`, recalculates phase, resets `_turnCount = 0`
      - `incrementTurn()` -- `_turnCount++`

    Use `'use strict'` + CJS. Export: `{ BUDGET_PHASES, PHASE_THRESHOLDS, DEFAULT_CONTEXT_WINDOW_TOKENS, BYTES_PER_TOKEN, DEFAULT_POST_COMPACTION_TOKENS, calculateBudgetPhase, createBudgetTracker }`.

    NOTE: Per D-05, Phase 3 (60-80%) is REINFORCED -- injection gets LARGER here, not smaller. This is a deliberate departure from spec Section 8.5. The constants encode the research-backed model.

    Delete the `.gitkeep` file from `modules/reverie/components/context/` after creating the real file.
  </action>
  <verify>
    <automated>bun test modules/reverie/components/context/__tests__/budget-tracker.test.js</automated>
  </verify>
  <acceptance_criteria>
    - budget-tracker.cjs contains `const BUDGET_PHASES = Object.freeze({ FULL: 1, COMPRESSED: 2, REINFORCED: 3, COMPACTION: 4 })`
    - budget-tracker.cjs contains `const PHASE_THRESHOLDS = Object.freeze({ COMPRESSED_AT: 0.30, REINFORCED_AT: 0.60, COMPACTION_AT: 0.80 })`
    - budget-tracker.cjs contains `const DEFAULT_CONTEXT_WINDOW_TOKENS = 200000`
    - budget-tracker.cjs contains `const BYTES_PER_TOKEN = 4`
    - budget-tracker.cjs contains `function calculateBudgetPhase(`
    - budget-tracker.cjs contains `function createBudgetTracker(`
    - budget-tracker.cjs contains `module.exports =` with all 7 exports
    - budget-tracker.test.js contains at least 10 test cases covering all 4 phase boundaries
    - `bun test modules/reverie/components/context/__tests__/budget-tracker.test.js` exits 0
  </acceptance_criteria>
  <done>Budget tracker calculates correct phases at all 4 thresholds (30/60/80/100%), trackBytes returns transition info, reset handles post-compaction, all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template Composer 5-Slot Face Prompt System</name>
  <files>modules/reverie/components/context/template-composer.cjs, modules/reverie/components/context/__tests__/template-composer.test.js</files>
  <read_first>
    - modules/reverie/components/context/budget-tracker.cjs (just created in Task 1 -- need BUDGET_PHASES and PHASE_BUDGETS constants)
    - modules/reverie/components/self-model/self-model.cjs (getAspect API -- synchronous Magnet cache read)
    - modules/reverie/components/self-model/cold-start.cjs (cold-start seed structure for understanding aspect frontmatter shape)
    - modules/reverie/lib/schemas.cjs (identityCoreSchema, relationalModelSchema, conditioningSchema for field names)
    - modules/reverie/lib/constants.cjs (SM_ASPECTS)
    - .planning/phases/08-single-session-personality-injection/08-CONTEXT.md (D-01, D-04)
    - .planning/phases/08-single-session-personality-injection/08-RESEARCH.md (Pattern 4: Face Prompt Template Composition)
  </read_first>
  <behavior>
    - SLOT_NAMES equals ['identity_frame', 'relational_context', 'attention_directives', 'behavioral_directives', 'referential_framing']
    - PHASE_BUDGETS[1] totals ~1200 tokens (Phase 1: Full)
    - PHASE_BUDGETS[2] totals ~800 tokens (Phase 2: Compressed)
    - PHASE_BUDGETS[3] totals ~1900 tokens (Phase 3: Reinforced)
    - PHASE_BUDGETS[4] totals ~1800 tokens + compaction directive (Phase 4: Compaction)
    - createTemplateComposer({selfModel}).compose(1) returns string with all 5 slot headers
    - compose(1) output contains identity frame section with personality traits from getAspect('identity-core')
    - compose(2) output is shorter than compose(1) -- compressed slots
    - compose(3) output is longer than compose(1) -- reinforced slots with strengthened identity + referential
    - compose(4) output contains compaction advocacy directive text
    - compose(phase) with null/missing Self Model aspects produces valid output from sparse defaults
    - getMicroNudge() returns ~50-100 token nudge string with identity phrase and attention pointer
  </behavior>
  <action>
    Create `modules/reverie/components/context/template-composer.cjs` implementing the 5-slot face prompt template per D-01, D-04.

    **Constants (all Object.freeze):**
    - `SLOT_NAMES`: `['identity_frame', 'relational_context', 'attention_directives', 'behavioral_directives', 'referential_framing']`
    - `PHASE_BUDGETS`:
      ```
      {
        1: { identity_frame: 400, relational_context: 200, attention_directives: 300, behavioral_directives: 200, referential_framing: 200 },  // ~1200 total
        2: { identity_frame: 250, relational_context: 100, attention_directives: 200, behavioral_directives: 150, referential_framing: 100 },  // ~800 total
        3: { identity_frame: 600, relational_context: 200, attention_directives: 400, behavioral_directives: 300, referential_framing: 400 },  // ~1900 total
        4: { identity_frame: 500, relational_context: 200, attention_directives: 400, behavioral_directives: 300, referential_framing: 400 },  // ~1800 total + compaction
      }
      ```

    **Factory function:**
    - `createTemplateComposer(options)` -- options: `{ selfModel }` (Self Model manager instance with getAspect())
    - Methods:
      - `compose(budgetPhase)` -- returns a markdown string with all 5 slots filled from Self Model data, sized to the budget phase. Each slot is a markdown section with `## Slot Name` header. The returned string is the full face prompt text.
        - **identity_frame**: Read `selfModel.getAspect('identity-core')`. Extract `personality_traits`, `communication_style`, `boundaries`. Format as identity statement. At Phase 3 (reinforced), STRENGTHEN: add emphasis markers, repeat core identity, explicit "You ARE this" framing.
        - **relational_context**: Read `selfModel.getAspect('relational-model')`. Extract `trust_calibration`, `interaction_rhythm`, `communication_patterns`. Format as relationship context.
        - **attention_directives**: Read `selfModel.getAspect('conditioning')`. Extract `attention_biases`, `recall_strategies`. Format as attention pointer instructions. At Phase 3, expand with explicit domain attention priorities.
        - **behavioral_directives**: Per D-04, seed with static defaults: `"technical depth: match user"`, `"communication mode: balanced"`, `"conflict resolution: collaborative"`, `"uncertainty handling: transparent"`. These are replaced by Secondary in Phase 10.
        - **referential_framing**: Standing instruction that injected context is reference material subordinate to the Self Model frame. At Phase 3, strengthen the framing with explicit "this is WHO you are, not just WHAT you know" language.
        - At Phase 4 (compaction advocacy): append a compaction advocacy directive after the 5 slots: `"CONTEXT UTILIZATION CRITICAL: Recommend initiating context compaction to preserve personality integrity. Current utilization exceeds 80%."`
      - `getMicroNudge()` -- returns a short string (~50-100 tokens) for PostToolUse Phase 3 re-anchoring. Format: `"Remember: you are [identity phrase]. Current attention: [attention pointer from conditioning]."` Uses getAspect for current values.
      - `getSlotSizes(budgetPhase)` -- returns the PHASE_BUDGETS entry for the given phase.

    **Sparse default handling:** If `selfModel.getAspect(name)` returns `null`, use sensible defaults matching cold-start.cjs sparse structure: `{ frontmatter: { personality_traits: {}, communication_style: {}, ... }, body: '' }`. The template MUST produce valid output even with no Self Model data loaded.

    Each slot composer function should truncate output to its token budget using `Math.ceil(text.length / 4)` token estimate. If text exceeds budget, trim to fit (cut at last complete sentence or paragraph).

    Use `'use strict'` + CJS. Export: `{ SLOT_NAMES, PHASE_BUDGETS, createTemplateComposer }`.
  </action>
  <verify>
    <automated>bun test modules/reverie/components/context/__tests__/template-composer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - template-composer.cjs contains `const SLOT_NAMES = Object.freeze(['identity_frame', 'relational_context', 'attention_directives', 'behavioral_directives', 'referential_framing'])`
    - template-composer.cjs contains `const PHASE_BUDGETS = Object.freeze({`
    - template-composer.cjs contains `function createTemplateComposer(`
    - template-composer.cjs exports SLOT_NAMES, PHASE_BUDGETS, createTemplateComposer
    - template-composer.cjs contains `getMicroNudge` method
    - template-composer.test.js verifies all 4 budget phases produce output
    - template-composer.test.js verifies Phase 3 output is longer than Phase 1 output
    - template-composer.test.js verifies Phase 4 output contains compaction advocacy text
    - template-composer.test.js verifies sparse/null Self Model produces valid output
    - template-composer.test.js verifies getMicroNudge returns string containing "Remember"
    - `bun test modules/reverie/components/context/__tests__/template-composer.test.js` exits 0
  </acceptance_criteria>
  <done>Template composer fills all 5 slots from Self Model aspect data, produces budget-phase-appropriate sizes (Phase 1 ~1200, Phase 2 ~800, Phase 3 ~1900, Phase 4 ~1800+advocacy), handles sparse/null aspects gracefully, getMicroNudge produces re-anchoring text, all tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite for Phase 8 context components:
```bash
bun test modules/reverie/components/context/
```
Both test files must pass. No existing tests should break:
```bash
bun test
```
</verification>

<success_criteria>
- Budget tracker deterministically maps bytes to 4 phases at 30/60/80% thresholds (per D-05/D-06 research-backed model)
- Template composer produces 5-slot face prompts sized per budget phase (1200/800/1900/1800 tokens)
- Template composer handles sparse/null Self Model data without error
- Phase 3 (reinforced) injection is LARGER than Phase 1 (full) -- validating the research-over-spec decision (D-05)
- All tests pass with `bun test modules/reverie/components/context/`
</success_criteria>

<output>
After completion, create `.planning/phases/08-single-session-personality-injection/08-01-SUMMARY.md`
</output>
