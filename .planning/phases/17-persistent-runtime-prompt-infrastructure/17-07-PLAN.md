---
phase: 17-persistent-runtime-prompt-infrastructure
plan: 07
type: execute
wave: 4
depends_on: [17-06]
files_modified:
  - modules/reverie/prompts/formation-attention-check.md
  - modules/reverie/prompts/formation-domain-id.md
  - modules/reverie/prompts/formation-body-system.md
  - modules/reverie/prompts/formation-body-user.md
  - modules/reverie/prompts/formation-meta-recall.md
  - modules/reverie/prompts/recall-passive-nudge.md
  - modules/reverie/prompts/recall-explicit.md
  - modules/reverie/prompts/formation-backfill.md
  - modules/reverie/components/formation/prompt-templates.cjs
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - "All 8 formation/recall/backfill prompt string literals are extracted to modules/reverie/prompts/*.md files"
    - "Each template has JSON frontmatter with name, version, slots (with required/type/default)"
    - "prompt-templates.cjs loads templates via Linotype instead of containing string literals"
    - "Templates produce identical output to the original string literals when cast with same context"
  artifacts:
    - path: "modules/reverie/prompts/formation-attention-check.md"
      provides: "Attention gate prompt template"
      contains: "attention-check"
    - path: "modules/reverie/prompts/formation-body-system.md"
      provides: "Body composition system prompt template"
      contains: "formation-body-system"
    - path: "modules/reverie/prompts/formation-body-user.md"
      provides: "Body composition user prompt template with conditionals and iteration"
      contains: "formation-body-user"
    - path: "modules/reverie/prompts/recall-passive-nudge.md"
      provides: "Passive recall nudge template"
      contains: "recall-passive-nudge"
    - path: "modules/reverie/components/formation/prompt-templates.cjs"
      provides: "Thin loader that uses Linotype cast() instead of string literals"
  key_links:
    - from: "modules/reverie/components/formation/prompt-templates.cjs"
      to: "lib/linotype/linotype.cjs"
      via: "require for parse/cast"
      pattern: "require.*linotype"
    - from: "modules/reverie/components/formation/prompt-templates.cjs"
      to: "modules/reverie/prompts/"
      via: "parse() calls for each template file"
      pattern: "prompts/"
---

<objective>
Extract formation, recall, and backfill prompt string literals to Linotype markdown templates.

Purpose: This is the first of two prompt extraction plans (per D-08). It covers the 8 prompts in prompt-templates.cjs (formation pipeline: attention check, domain ID, body composition system+user, meta-recall, plus recall: passive nudge, explicit reconstruction, plus backfill). Each string literal becomes a markdown file in modules/reverie/prompts/ with JSON frontmatter declaring slots. The .cjs file becomes a thin loader that parses templates and exports functions that call linotype.cast() with prepared context (per D-09 hybrid pattern).

Output: 8 markdown template files in modules/reverie/prompts/, prompt-templates.cjs rewritten as thin template loader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-CONTEXT.md
@.claude/unfuck-the-whole-damn-thing.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-02-SUMMARY.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-06-SUMMARY.md
@modules/reverie/components/formation/prompt-templates.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create formation and recall prompt template files</name>
  <files>modules/reverie/prompts/formation-attention-check.md, modules/reverie/prompts/formation-domain-id.md, modules/reverie/prompts/formation-body-system.md, modules/reverie/prompts/formation-body-user.md, modules/reverie/prompts/formation-meta-recall.md, modules/reverie/prompts/recall-passive-nudge.md, modules/reverie/prompts/recall-explicit.md, modules/reverie/prompts/formation-backfill.md</files>
  <read_first>
    - modules/reverie/components/formation/prompt-templates.cjs (FULL read -- every line. Contains all string literals and user() functions to extract)
    - .claude/unfuck-the-whole-damn-thing.md lines 660-732 (hybrid pattern specification for function-based prompts)
    - .claude/unfuck-the-whole-damn-thing.md lines 1027-1049 (prompt extraction inventory with line numbers)
  </read_first>
  <action>
Create modules/reverie/prompts/ directory.

For each prompt in prompt-templates.cjs, extract the string literal content into a markdown template file. The exact content MUST match the original string literals. Do not rephrase, reword, or add/remove content. Wrap dynamic parts in template syntax.

Each template file follows this structure: JSON frontmatter block (delimited by triple-dash lines) containing name, version, description, tags, token_estimate, and slots object. Then the template body with Linotype syntax (double-brace variables, conditionals, iteration).

Frontmatter fields per template: `name` (string, matches filename without .md), `version` ("1.0"), `slots` (object mapping slot names to objects with required boolean, type string, optional default). Optional fields: `description`, `tags`, `token_estimate`.

**1. formation-attention-check.md** (from prompt-templates.cjs attention_check):
- Extract the `system` string as the template body.
- Slots: identify any dynamic parts in the system string. If pure static, declare no required slots.
- The `user()` function logic: identify slots needed for user message: `user_prompt`, `tools_used`, `tool_results`.
- Since system and user are separate concerns in the formation pipeline, create this as the SYSTEM template. The user context preparation stays in code.

**2. formation-domain-id.md** (from prompt-templates.cjs domain_identification):
- Extract `system` string.
- User function slots: `user_prompt`, `domains` (array).

**3. formation-body-system.md** (from prompt-templates.cjs body_composition system):
- Extract `system` string.
- This is typically a static instruction set -- may have zero slots.

**4. formation-body-user.md** (from prompt-templates.cjs body_composition user):
- This is the most complex -- the user() function has conditionals.
- Template uses `{{#if}}` and `{{#each}}` for dynamic parts.
- Slots: `user_prompt` (required), `domain` (required), `self_context` (optional), `recall_fragments` (optional, type array).

**5. formation-meta-recall.md** (from prompt-templates.cjs meta_recall_reflection):
- Extract system and user template content.
- Slots: `fragment_body`, `fragment_frame`, `user_prompt`.

**6. recall-passive-nudge.md** (from prompt-templates.cjs passive_nudge):
- Extract system string.
- Slots: `fragments` (array), `user_prompt` (optional).

**7. recall-explicit.md** (from prompt-templates.cjs explicit_reconstruction):
- Extract system string.
- Slots: `query`, `fragments` (array).

**8. formation-backfill.md** (from prompt-templates.cjs backfill_formation):
- Extract system string.
- Slots: `conversation_context`, `backfill_prompt`.

CRITICAL: Read the ACTUAL content of each string literal from prompt-templates.cjs. Do not paraphrase. The template body must produce character-identical output to the original when cast with the same context values.
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && ls modules/reverie/prompts/*.md | wc -l && bun -e "const l = require('./lib/linotype/linotype.cjs'); const m = l.parseString(require('fs').readFileSync('modules/reverie/prompts/formation-attention-check.md','utf8'),'test'); console.log(m.name, Object.keys(m.slots).length >= 0 ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - modules/reverie/prompts/ directory exists with 8 .md files
    - Each file has JSON frontmatter between triple-dash delimiters (not YAML)
    - Each file's frontmatter has name, version, slots fields
    - formation-attention-check.md frontmatter name is "formation-attention-check"
    - formation-body-user.md contains {{#if}} and/or {{#each}} syntax for conditional content
    - recall-passive-nudge.md contains {{#each}} for fragment list rendering
    - All 8 files parseable by linotype.parseString without errors
    - Template content matches original string literals (no rewording)
  </acceptance_criteria>
  <done>All 8 formation/recall/backfill prompts extracted to markdown templates with JSON frontmatter and appropriate template syntax</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite prompt-templates.cjs as thin Linotype loader</name>
  <files>modules/reverie/components/formation/prompt-templates.cjs, modules/reverie/manifest.cjs</files>
  <read_first>
    - modules/reverie/components/formation/prompt-templates.cjs (full file -- understand current export shape: PROMPT_TEMPLATES object with template keys)
    - modules/reverie/prompts/ (all 8 template files from Task 1)
    - lib/linotype/linotype.cjs (parse, cast, parseString API)
    - modules/reverie/manifest.cjs (current manifest -- adding templates section)
    - modules/reverie/components/formation/formation-pipeline.cjs (consumer of prompt-templates -- understand how templates are used)
    - modules/reverie/components/recall/recall-engine.cjs (consumer of recall templates)
  </read_first>
  <action>
Rewrite modules/reverie/components/formation/prompt-templates.cjs:

The current file exports a PROMPT_TEMPLATES object where each key has `{ system: string, user: function }`. The new file:

1. Load Linotype: `const linotype = require('../../../../lib/linotype/linotype.cjs')`.

2. Load all template Matrices on module load (synchronous via parseString since templates are local files):
   ```javascript
   const fs = require('node:fs');
   const path = require('node:path');
   const PROMPTS_DIR = path.join(__dirname, '../../prompts');

   function _loadTemplate(filename) {
     const content = fs.readFileSync(path.join(PROMPTS_DIR, filename), 'utf8');
     return linotype.parseString(content, filename);
   }

   const _matrices = {
     attention_check: _loadTemplate('formation-attention-check.md'),
     domain_identification: _loadTemplate('formation-domain-id.md'),
     body_composition_system: _loadTemplate('formation-body-system.md'),
     body_composition_user: _loadTemplate('formation-body-user.md'),
     meta_recall: _loadTemplate('formation-meta-recall.md'),
     passive_nudge: _loadTemplate('recall-passive-nudge.md'),
     explicit_reconstruction: _loadTemplate('recall-explicit.md'),
     backfill: _loadTemplate('formation-backfill.md'),
   };
   ```

3. Export PROMPT_TEMPLATES with the SAME shape as before, so existing consumers (formation-pipeline, recall-engine) do not break:
   ```javascript
   const PROMPT_TEMPLATES = {
     attention_check: {
       system: (context) => linotype.cast(_matrices.attention_check, context || {}).content,
       user: (stimulus) => {
         // Context preparation logic stays in code (per D-09 hybrid pattern)
         // Prepare context object from stimulus, then:
         // return prepared string (same logic as original user() function)
       }
     },
     // ... same pattern for each template
   };
   ```

4. CRITICAL: The export interface MUST remain backward compatible. Existing code calls `PROMPT_TEMPLATES.attention_check.system` (as string or function) and `.user(stimulus, ...)`. If `system` was a string before, make it a getter or function that returns the cast result. If consumers call it as a function, keep function form.

5. The `user()` functions contain context preparation logic (conditional joins, data transforms). This logic stays in code. The template provides structure. Code prepares context, calls `linotype.cast(_matrices.xxx_user, preparedContext).content`.

6. Ensure Object.freeze on the exported PROMPT_TEMPLATES object.

Update modules/reverie/manifest.cjs -- add `templates` section to the exported manifest:
```javascript
templates: {
  directory: 'prompts',
  namespace: 'reverie'
}
```
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && bun test modules/reverie/components/formation/ --timeout 15000</automated>
  </verify>
  <acceptance_criteria>
    - prompt-templates.cjs requires lib/linotype/linotype.cjs
    - prompt-templates.cjs loads templates from modules/reverie/prompts/ directory
    - PROMPT_TEMPLATES export has same keys as before: attention_check, domain_identification, body_composition, meta_recall_reflection, passive_nudge, explicit_reconstruction, backfill_formation
    - Each template key has system and user properties (backward compatible interface)
    - manifest.cjs has templates: { directory: 'prompts', namespace: 'reverie' }
    - Existing formation pipeline tests pass (no regression in template access pattern)
    - No string literals > 100 chars remain in prompt-templates.cjs (all moved to .md files)
  </acceptance_criteria>
  <done>prompt-templates.cjs loads from Linotype templates, maintains backward-compatible export shape. manifest.cjs declares template directory. Formation/recall pipelines work unchanged.</done>
</task>

</tasks>

<verification>
- `bun test modules/reverie/components/formation/ --timeout 15000` passes
- `ls modules/reverie/prompts/*.md | wc -l` returns 8
- All templates parseable by Linotype
- No prompt string literals > 100 chars in prompt-templates.cjs
</verification>

<success_criteria>
8 formation/recall/backfill prompts extracted to markdown templates. prompt-templates.cjs uses Linotype parse/cast. Backward-compatible export interface. Existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/17-persistent-runtime-prompt-infrastructure/17-07-SUMMARY.md`
</output>
