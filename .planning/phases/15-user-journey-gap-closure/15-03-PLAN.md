---
phase: 15-user-journey-gap-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - modules/reverie/skills/dynamo-skill.cjs
  - modules/reverie/skills/reverie-skill.cjs
  - modules/reverie/skills/validate-skill.cjs
  - modules/reverie/skills/skill-content.test.cjs
  - .claude/agents/reverie-formation.md
autonomous: true
requirements:
  - INT-01
  - INT-02
must_haves:
  truths:
    - "Every command referenced in a skill .md file maps to a real registered Pulley command"
    - "Skills use transparent CLI invocations: `bun bin/dynamo.cjs reverie <cmd>` format"
    - "/reverie skill references start and stop commands that now exist"
    - "Formation agent output schema matches what handleSubagentStop parses"
    - "Skill content tests validate all command references against the actual command list"
  artifacts:
    - path: "modules/reverie/skills/dynamo-skill.cjs"
      provides: "Rewritten /dynamo skill content"
      contains: "bun bin/dynamo.cjs status"
    - path: "modules/reverie/skills/reverie-skill.cjs"
      provides: "Rewritten /reverie skill content"
      contains: "bun bin/dynamo.cjs reverie start"
    - path: "modules/reverie/skills/validate-skill.cjs"
      provides: "Rewritten /dynamo-validate skill content"
      contains: "bun test"
    - path: "modules/reverie/skills/skill-content.test.cjs"
      provides: "Updated tests validating skill command references"
      contains: "reverie start"
    - path: ".claude/agents/reverie-formation.md"
      provides: "Audited formation agent definition"
  key_links:
    - from: "modules/reverie/skills/reverie-skill.cjs"
      to: "modules/reverie/components/cli/register-commands.cjs"
      via: "skill content references commands registered in register-commands.cjs"
      pattern: "reverie start|reverie stop|reverie status"
---

<objective>
Rewrite all three skill .md content modules from the actual CLI command surface as ground truth. Audit formation agent definition against handleSubagentStop parsing. Update skill content tests to validate against the complete command list including start/stop.

Purpose: Skills currently promise actions that don't exist (start/stop before Plan 01). After Plan 01 adds those commands, this plan rewrites all skills to match reality and audits the formation agent for output schema mismatches.
Output: Three rewritten skill content modules, updated skill tests, audited formation agent definition
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-user-journey-gap-closure/15-CONTEXT.md
@.planning/phases/15-user-journey-gap-closure/15-RESEARCH.md

<interfaces>
<!-- Complete command list (ground truth from Research, verified against register-commands.cjs + platform-commands.cjs). -->
<!-- This is the ONLY source of truth for skill content. -->

Platform commands (core/sdk/pulley/platform-commands.cjs):
  bun bin/dynamo.cjs status          # Platform status
  bun bin/dynamo.cjs health          # Service health check
  bun bin/dynamo.cjs version         # Version info
  bun bin/dynamo.cjs install <url>   # Install plugin/module
  bun bin/dynamo.cjs update          # Self-update
  bun bin/dynamo.cjs config [key]    # Show configuration

Reverie commands (modules/reverie/components/cli/register-commands.cjs):
  bun bin/dynamo.cjs reverie status                    # Operational dashboard
  bun bin/dynamo.cjs reverie start                     # Upgrade to Active mode (NEW, Plan 01)
  bun bin/dynamo.cjs reverie stop                      # Graceful shutdown with REM (NEW, Plan 01)
  bun bin/dynamo.cjs reverie inspect fragment <id>     # Inspect specific fragment
  bun bin/dynamo.cjs reverie inspect domains           # List domains
  bun bin/dynamo.cjs reverie inspect associations <e>  # Association graph
  bun bin/dynamo.cjs reverie inspect self-model        # Full Self Model
  bun bin/dynamo.cjs reverie inspect identity          # Identity Core
  bun bin/dynamo.cjs reverie inspect relational        # Relational Model
  bun bin/dynamo.cjs reverie inspect conditioning      # Conditioning
  bun bin/dynamo.cjs reverie history sessions          # Session timeline
  bun bin/dynamo.cjs reverie history fragments         # Fragment timeline
  bun bin/dynamo.cjs reverie history consolidations    # REM events
  bun bin/dynamo.cjs reverie reset fragments --confirm # Wipe fragments
  bun bin/dynamo.cjs reverie reset self-model --confirm# Reset Self Model
  bun bin/dynamo.cjs reverie reset all --confirm       # Factory reset
  bun bin/dynamo.cjs reverie backfill <file> [--dry-run] [--limit N] [--batch-size N]

From modules/reverie/skills/reverie-skill.cjs (existing pattern):
```javascript
'use strict';
const REVERIE_SKILL_CONTENT = `...`;
function registerReverieSkill(exciter) {
  return exciter.registerSkill('reverie', { description: '...', content: REVERIE_SKILL_CONTENT });
}
module.exports = { registerReverieSkill, REVERIE_SKILL_CONTENT };
```

From modules/reverie/hooks/hook-handlers.cjs handleSubagentStop (formation parsing):
```javascript
// handleSubagentStop (line ~531-583):
// 1. Filters by agent_name === 'reverie-formation'
// 2. Reads latest-output.json from FORMATION_OUTPUT_DIR
// 3. Passes raw JSON to formationPipeline.processFormationOutput()
// formationPipeline.processFormationOutput delegates to fragment-assembler.cjs parseFormationOutput()
```

From modules/reverie/components/formation/fragment-assembler.cjs:
```javascript
// parseFormationOutput(rawOutput) expects:
// { should_form: boolean, attention_reasoning: string, fragments: Array<{
//   formation_frame: string, domains: string[], entities: string[],
//   attention_tags: string[], self_model_relevance: object,
//   emotional_valence: number, initial_weight: number, body: string,
//   source_locator: object|null, source_fragments: string[]
// }>, nudge: string|null }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite all three skill content modules and update tests</name>
  <files>modules/reverie/skills/dynamo-skill.cjs, modules/reverie/skills/reverie-skill.cjs, modules/reverie/skills/validate-skill.cjs, modules/reverie/skills/skill-content.test.cjs</files>
  <read_first>
    - modules/reverie/skills/dynamo-skill.cjs (current content, registration pattern)
    - modules/reverie/skills/reverie-skill.cjs (current content, registration pattern)
    - modules/reverie/skills/validate-skill.cjs (current content, registration pattern)
    - modules/reverie/skills/skill-content.test.cjs (current test assertions)
    - modules/reverie/components/cli/register-commands.cjs (verify complete command list after Plan 01)
    - core/sdk/pulley/platform-commands.cjs (verify platform command list)
  </read_first>
  <action>
    Per D-07, D-08, D-09: Rewrite all three skill content modules. Every command referenced in a skill MUST exist in register-commands.cjs or platform-commands.cjs. Skills use transparent `bun bin/dynamo.cjs ...` format per D-08 and Phase 12.1 D-03.

    **Rewrite `modules/reverie/skills/dynamo-skill.cjs` -- /dynamo skill:**

    The /dynamo skill is a platform dashboard. Content should cover:
    - Check platform health: `bun bin/dynamo.cjs health`
    - View platform status: `bun bin/dynamo.cjs status`
    - View version: `bun bin/dynamo.cjs version`
    - View configuration: `bun bin/dynamo.cjs config`

    Structure: Title, brief description of what /dynamo does, numbered steps with CLI commands. Keep it concise -- this is a reference card, not documentation.

    Preserve the existing `registerDynamoSkill(exciter)` function and `module.exports = { registerDynamoSkill, DYNAMO_SKILL_CONTENT }` export pattern.

    **Rewrite `modules/reverie/skills/reverie-skill.cjs` -- /reverie skill:**

    The /reverie skill is a session management hub. Content should cover:
    - Check status: `bun bin/dynamo.cjs reverie status`
    - Start Active mode: `bun bin/dynamo.cjs reverie start`
    - Stop with REM: `bun bin/dynamo.cjs reverie stop`
    - Inspect memory: inspect subcommands (fragment, domains, associations, self-model, identity, relational, conditioning)
    - View history: history subcommands (sessions, fragments, consolidations)
    - Reset: reset subcommands (fragments, self-model, all) with --confirm note
    - Backfill: `bun bin/dynamo.cjs reverie backfill <file>` with flag descriptions

    Structure: Title, brief description, grouped by operation type (Status, Session Control, Inspect, History, Reset, Backfill). Each group has the relevant CLI commands.

    Preserve the existing `registerReverieSkill(exciter)` function and `module.exports = { registerReverieSkill, REVERIE_SKILL_CONTENT }` export pattern.

    **Rewrite `modules/reverie/skills/validate-skill.cjs` -- /dynamo-validate skill:**

    The /dynamo-validate skill runs the validation suite. Content should cover:
    - Run all Reverie validation tests: `bun test modules/reverie/validation/`
    - Run specific test files (list key ones): e.g., `bun test modules/reverie/validation/integration-harness.test.cjs`
    - What validation covers (briefly): spec compliance, integration seams, platform integration

    Preserve the existing `registerValidateSkill(exciter)` function and `module.exports = { registerValidateSkill, VALIDATE_SKILL_CONTENT }` export pattern.

    **Also audit help text (D-09):** For each command referenced in the skills, verify the `description` field passed to `circuitApi.registerCommand()` in register-commands.cjs and platform-commands.cjs is accurate. If any description is misleading or wrong, fix it in the source file.

    **Update `modules/reverie/skills/skill-content.test.cjs`:**

    Update test assertions to match the rewritten skill content. Tests should verify:
    - DYNAMO_SKILL_CONTENT contains 'bun bin/dynamo.cjs status'
    - DYNAMO_SKILL_CONTENT contains 'bun bin/dynamo.cjs health'
    - REVERIE_SKILL_CONTENT contains 'bun bin/dynamo.cjs reverie status'
    - REVERIE_SKILL_CONTENT contains 'bun bin/dynamo.cjs reverie start'
    - REVERIE_SKILL_CONTENT contains 'bun bin/dynamo.cjs reverie stop'
    - REVERIE_SKILL_CONTENT contains 'reverie inspect'
    - REVERIE_SKILL_CONTENT contains 'reverie history'
    - REVERIE_SKILL_CONTENT contains 'reverie reset'
    - REVERIE_SKILL_CONTENT contains 'reverie backfill'
    - VALIDATE_SKILL_CONTENT contains 'bun test'
    - No skill content references a command that doesn't exist (cross-reference against a COMMANDS array)
    - All three export functions exist: registerDynamoSkill, registerReverieSkill, registerValidateSkill
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && bun test modules/reverie/skills/skill-content.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - dynamo-skill.cjs DYNAMO_SKILL_CONTENT contains 'bun bin/dynamo.cjs status'
    - dynamo-skill.cjs DYNAMO_SKILL_CONTENT contains 'bun bin/dynamo.cjs health'
    - dynamo-skill.cjs DYNAMO_SKILL_CONTENT contains 'bun bin/dynamo.cjs version'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'bun bin/dynamo.cjs reverie start'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'bun bin/dynamo.cjs reverie stop'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'bun bin/dynamo.cjs reverie status'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'reverie inspect'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'reverie history'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'reverie reset'
    - reverie-skill.cjs REVERIE_SKILL_CONTENT contains 'reverie backfill'
    - validate-skill.cjs VALIDATE_SKILL_CONTENT contains 'bun test'
    - skill-content.test.cjs exits 0
    - All three files preserve their existing registerXxxSkill export functions
  </acceptance_criteria>
  <done>All three skill content modules rewritten from CLI ground truth. Every command reference maps to a real Pulley command. Tests validate command coverage. Help text audited.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix formation agent definition</name>
  <files>.claude/agents/reverie-formation.md</files>
  <read_first>
    - .claude/agents/reverie-formation.md (current agent definition -- full file)
    - modules/reverie/hooks/hook-handlers.cjs (handleSubagentStop function -- find formation output parsing, FORMATION_OUTPUT_DIR constant)
    - modules/reverie/components/formation/fragment-assembler.cjs (parseFormationOutput function -- exact field expectations)
    - modules/reverie/components/formation/formation-pipeline.cjs (processFormationOutput -- how it calls parseFormationOutput)
    - modules/reverie/hooks/hook-handlers.cjs (FORMATION_STIMULUS_DIR, FORMATION_OUTPUT_DIR constants)
  </read_first>
  <action>
    Per D-12: Audit the formation agent definition against what handleSubagentStop and fragment-assembler.cjs actually parse. Fix any mismatches in-place.

    **Audit checklist:**

    1. **Output schema alignment:** Compare the JSON output schema specified in the agent .md file field-by-field against `parseFormationOutput()` in fragment-assembler.cjs. Check:
       - Top-level fields: `should_form` (boolean), `attention_reasoning` (string), `fragments` (array), `nudge` (string|null)
       - Fragment fields: `formation_frame` (string), `domains` (string[]), `entities` (string[]), `attention_tags` (string[]), `self_model_relevance` (object), `emotional_valence` (number), `initial_weight` (number), `body` (string), `source_locator` (object|null), `source_fragments` (string[])
       - Field names must match exactly (case-sensitive)
       - Required vs optional must match
       - Any field in the agent schema but not in parseFormationOutput is noise -- remove it
       - Any field in parseFormationOutput but not in the agent schema is missing -- add it

    2. **File path conventions:** The agent reads stimulus from FORMATION_STIMULUS_DIR and writes output to FORMATION_OUTPUT_DIR. Verify the agent definition references these paths correctly.

    3. **Tool permissions:** Agent specifies `tools` and `permissionMode`. Per Research Open Question 3, check if Bash is needed. Formation agent reads a stimulus file and writes JSON output. If it only needs Read + Write, remove Bash for principle of least privilege. If Bash is actually used in the agent prompt for any reason, keep it.

    4. **Model specification:** Check if `model` field matches what sessionConfig or formation pipeline expects (typically `sonnet` for background agents).

    5. **Background mode:** Verify `background: true` is set (formation is a fire-and-forget subagent).

    Fix any mismatches found. If the agent definition is already correct, document that in the summary (no changes needed is a valid audit outcome).
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && grep -c "should_form\|attention_reasoning\|fragments\|formation_frame\|nudge" .claude/agents/reverie-formation.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/reverie-formation.md contains `should_form` field
    - .claude/agents/reverie-formation.md contains `attention_reasoning` field
    - .claude/agents/reverie-formation.md contains `fragments` field
    - .claude/agents/reverie-formation.md contains `formation_frame` field in fragment schema
    - .claude/agents/reverie-formation.md contains `nudge` field
    - .claude/agents/reverie-formation.md output schema field names match parseFormationOutput() field names exactly
    - .claude/agents/reverie-formation.md contains `background: true` (or equivalent)
  </acceptance_criteria>
  <done>Formation agent definition audited against handleSubagentStop parsing and fragment-assembler.cjs. All output schema fields match. Tool permissions reviewed. Mismatches fixed or confirmed aligned.</done>
</task>

</tasks>

<verification>
- `bun test modules/reverie/skills/skill-content.test.cjs` passes
- No skill content references commands not in register-commands.cjs or platform-commands.cjs
- Formation agent output schema fields match parseFormationOutput() expectations
</verification>

<success_criteria>
- All three skill content modules rewritten from CLI ground truth
- Every command referenced in a skill maps to a real registered command
- Skills use transparent `bun bin/dynamo.cjs ...` format
- Formation agent output schema matches parsing expectations
- Skill content tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-user-journey-gap-closure/15-03-SUMMARY.md`
</output>
