# Phase 3.2: Assay Federated Search - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-03-23
**Phase:** 03.2-assay-federated-search
**Areas discussed:** Provider registration, Query interface, Result merging, Query translation, Indexing scope
**Mode:** Auto (--auto flag)

---

## Provider Registration

| Option | Description | Selected |
|--------|-------------|----------|
| Options-based DI at init() | Providers injected via options object, consistent with all services | auto |
| Registry/container lookup | Assay discovers providers through Armature container | |
| Self-discovery | Assay scans for provider implementations at startup | |

**User's choice:** [auto] Options-based DI at init() (recommended default)
**Notes:** Consistent with Phases 1-3.1 established pattern. Armature can add container resolution in Phase 4.

---

## Query Interface Design

| Option | Description | Selected |
|--------|-------------|----------|
| Dual interface (search + searchProvider) | Federated search() for multi-provider, searchProvider() for targeted | auto |
| Single search() with provider filter | One method, providers param restricts scope | |
| Per-provider methods (searchLedger, searchJournal) | Explicit methods per provider | |

**User's choice:** [auto] Dual interface (recommended default)
**Notes:** Satisfies both ROADMAP success criteria. search() for recall pipeline, searchProvider() for Tertiary's targeted Ledger scans.

---

## Result Merging Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Provider-grouped with metadata | Flat results array + per-provider summary, consumer ranks | auto |
| Interleaved by relevance score | Assay applies cross-provider relevance ranking | |
| Grouped by provider | Separate result arrays per provider | |

**User's choice:** [auto] Provider-grouped with metadata (recommended default)
**Notes:** Reverie owns ranking via Self Model relevance weighting. Assay returns raw results, Mind scores them.

---

## Query Translation (Provider-Specific Optimization)

| Option | Description | Selected |
|--------|-------------|----------|
| Capability declaration | Providers declare what they can optimize, Assay routes accordingly | auto |
| Internal knowledge | Assay has hardcoded knowledge of Ledger=SQL, Journal=frontmatter | |
| Query dialect hints | Query object carries per-provider hints, Assay passes through | |

**User's choice:** [auto] Capability declaration (recommended default)
**Notes:** Extensible for future providers (Tika, Meilisearch) without modifying Assay core.

---

## Indexing Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Federated query only (no indexing in M1) | Assay routes and merges, indexing deferred to M2 | auto |
| Basic indexing now | Build simple in-memory index for frequent queries | |
| Full indexing infrastructure | Tika/Meilisearch/Opensearch integration | |

**User's choice:** [auto] Federated query only (recommended default)
**Notes:** Architecture plan mentions indexing tools but Reverie recall volumes in M2 will drive the need.

---

## Claude's Discretion

- Query plan optimization heuristics
- Provider capability schema format
- Result ordering within flat array
- Timing measurement implementation
- Individual provider failure handling during federation
- healthCheck() aggregation strategy

## Deferred Ideas

- Full-text indexing (Tika/Meilisearch/Opensearch) -- M2+
- Cross-provider relevance ranking -- Reverie concern
- Index management and optimization -- M2
- Query result caching -- premature without usage patterns
