---
phase: 17-persistent-runtime-prompt-infrastructure
plan: 10
type: execute
wave: 5
depends_on: [17-07, 17-08, 17-09]
files_modified: []
autonomous: false
requirements: [INF-01, SVC-01, SVC-08, MOD-01]
must_haves:
  truths:
    - "bun bin/dynamo.cjs start launches daemon and reports PID/port"
    - "bun bin/dynamo.cjs status shows daemon health with loaded modules"
    - "bun bin/dynamo.cjs hook SessionStart returns {} when module not enabled"
    - "bun bin/dynamo.cjs reverie enable activates module (mock or real triad)"
    - "Hook dispatch through daemon returns face prompt additionalContext"
    - "bun bin/dynamo.cjs stop gracefully shuts down daemon"
    - "All 15 Linotype templates parse and cast without errors"
    - "All existing test suites pass (no regression)"
  artifacts:
    - path: ".dynamo/daemon.json"
      provides: "Runtime PID file created by daemon start"
      contains: "pid"
    - path: "modules/reverie/prompts/"
      provides: "15 markdown template files"
  key_links:
    - from: "bin/dynamo.cjs"
      to: "core/daemon-server.cjs"
      via: "HTTP fetch to /hook, /cli, /health, /shutdown"
      pattern: "fetch.*localhost"
    - from: "core/daemon-server.cjs"
      to: "core/services/exciter/exciter.cjs"
      via: "dispatchHook for hook routing"
      pattern: "dispatchHook"
    - from: "modules/reverie/components/formation/prompt-templates.cjs"
      to: "modules/reverie/prompts/"
      via: "Linotype parse/cast"
      pattern: "linotype"
---

<objective>
End-to-end validation of daemon runtime and Linotype prompt infrastructure.

Purpose: This is the Wave 5 validation plan. It runs the full acceptance test described in the PRD: daemon start, module enable, hook dispatch through daemon, status check, graceful shutdown. It also validates all 15 Linotype templates produce correct output and runs the full existing test suite for regression checking. This plan has a human-verify checkpoint because the acceptance test involves observing terminal behavior.

Output: Validated E2E daemon lifecycle, validated Linotype templates, no regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-CONTEXT.md
@.claude/unfuck-the-whole-damn-thing.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-07-SUMMARY.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-08-SUMMARY.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated E2E daemon lifecycle and template validation</name>
  <files></files>
  <read_first>
    - core/daemon.cjs (daemon entry point)
    - core/daemon-server.cjs (HTTP server)
    - core/daemon-lifecycle.cjs (lifecycle utilities)
    - bin/dynamo.cjs (thin client)
    - .claude/unfuck-the-whole-damn-thing.md lines 1217-1253 (acceptance test specification)
  </read_first>
  <action>
Run the automated portion of the E2E acceptance test:

1. Daemon start test: Clean stale state (rm -rf .dynamo/), run `bun bin/dynamo.cjs start`, verify .dynamo/daemon.json exists with PID, verify GET http://localhost:9876/health returns { status: 'running' }.

2. Hook dispatch with no module enabled (off-ramp state 3): `echo '{}' | bun bin/dynamo.cjs hook SessionStart` should output {} (empty, no module enabled).

3. Status check: `bun bin/dynamo.cjs status` should show running, PID, port, uptime.

4. Module enable: `bun bin/dynamo.cjs reverie enable` should show enabled or document error.

5. Linotype template validation: Parse all 15 .md files in modules/reverie/prompts/ via linotype.parseString, run validate() on each, report any parse or validation failures.

6. Graceful shutdown: `bun bin/dynamo.cjs stop`, verify .dynamo/daemon.json is removed.

7. Hook with daemon off (off-ramp state 1): `echo '{}' | bun bin/dynamo.cjs hook SessionStart` should output {} with exit 0.

8. Regression test suite: `bun test --timeout 30000`, all existing tests must pass.

Document all results in the SUMMARY. If any step fails, document the failure and what needs fixing (gap closure candidate).
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && bun test --timeout 60000 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Daemon starts and .dynamo/daemon.json created with valid PID
    - GET /health returns { status: 'running' } with correct PID
    - Hook with no module returns {} exit 0
    - Hook with daemon off returns {} exit 0
    - All 15 templates parse without errors
    - bun bin/dynamo.cjs stop removes .dynamo/daemon.json
    - Full test suite passes with 0 failures (or documents known failures for gap closure)
  </acceptance_criteria>
  <done>Automated E2E tests pass for daemon lifecycle, hook off-ramp, template validation, and regression suite</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of daemon runtime behavior</name>
  <files></files>
  <action>
  Present the human verification checklist. All automated work is complete. The user verifies observable daemon behavior.
  </action>
  <what-built>
  Complete daemon runtime and Linotype prompt infrastructure:
  - Daemon process (start/stop/status via thin client)
  - HTTP server with hook, CLI, Wire, and management routes
  - Linotype template library (parse, cast, compose, validate, inspect)
  - 15 Reverie prompt templates extracted from .cjs string literals
  - Session identity dispatch in hook handlers
  - Module lifecycle (enable/disable)
  - Skills updated for daemon commands
  </what-built>
  <how-to-verify>
  1. Open a terminal and run: `bun bin/dynamo.cjs start`
     - Expected: "Dynamo running (PID XXXX, port 9876)"
     - Verify: `.dynamo/daemon.json` exists

  2. Run: `bun bin/dynamo.cjs status`
     - Expected: daemon health showing running, PID, port, uptime

  3. Run: `echo '{}' | bun bin/dynamo.cjs hook SessionStart`
     - Expected: `{}` (no module enabled, silent)

  4. Run: `bun bin/dynamo.cjs stop`
     - Expected: "Dynamo stopped."
     - Verify: `.dynamo/daemon.json` removed

  5. Run: `echo '{}' | bun bin/dynamo.cjs hook SessionStart`
     - Expected: `{}` (daemon not running, silent)
     - Verify: exit code is 0 (no error)

  6. Check modules/reverie/prompts/ directory:
     - Expected: 15 .md files with readable prompt templates

  7. Run: `bun test --timeout 60000`
     - Expected: all tests pass
  </how-to-verify>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated check"</automated>
  </verify>
  <done>User confirms daemon lifecycle, hook off-ramp, and template extraction are working correctly</done>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Full E2E lifecycle: start -> status -> hook -> stop -> hook (daemon off)
- All 15 templates parse and validate
- Full test suite passes
- Human verification of daemon behavior
</verification>

<success_criteria>
E2E daemon lifecycle works: start spawns persistent daemon, hooks route through daemon, stop shuts down gracefully. All 15 Linotype templates valid. Full regression suite passes. Human verifies observable behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/17-persistent-runtime-prompt-infrastructure/17-10-SUMMARY.md`
</output>
