---
phase: 03-data-providers-infrastructure-services
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - core/services/conductor/conductor.cjs
  - core/services/conductor/__tests__/conductor.test.js
autonomous: true
requirements: [SVC-06]

must_haves:
  truths:
    - "Conductor manages Docker Compose lifecycle (up/down/status) for containerized services"
    - "Conductor checks dependency health (Bun version, git availability, DuckDB loadable, disk space)"
    - "Conductor gracefully degrades when Docker is not installed"
    - "Conductor reports infrastructure status through healthCheck"
  artifacts:
    - path: "core/services/conductor/conductor.cjs"
      provides: "Conductor infrastructure service factory"
      exports: ["createConductor"]
  key_links:
    - from: "core/services/conductor/conductor.cjs"
      to: "Bun.spawnSync"
      via: "docker compose CLI execution"
      pattern: "Bun\\.spawnSync.*docker"
    - from: "core/services/conductor/conductor.cjs"
      to: "core/services/switchboard/switchboard.cjs"
      via: "options.switchboard for infrastructure events"
      pattern: "options\\.switchboard"
---

<objective>
Build the Conductor infrastructure management service: manages Docker Compose lifecycle for containerized services (MCP servers, databases), checks platform dependency health (Bun version, git, DuckDB, disk space), and gracefully degrades when Docker is absent per D-07.

Purpose: Conductor is the infrastructure primitive layer. It ensures external dependencies are available and manages the lifecycle of containerized services that Wire and other components need. Graceful degradation means the platform works without Docker installed -- Conductor simply reports what is and is not available.

Output: Conductor service factory, full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-providers-infrastructure-services/03-CONTEXT.md
@.planning/phases/03-data-providers-infrastructure-services/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/result.cjs:
```javascript
function ok(value) { return { ok: true, value }; }
function err(code, message, context) { return { ok: false, error: { code, message, context } }; }
```

From lib/contract.cjs:
```javascript
function createContract(name, shape, implementation) {
  // validates all shape.required are functions on implementation
  // returns ok(Object.freeze({ ...implementation }))
}
```

From core/services/switchboard/switchboard.cjs:
```javascript
function createSwitchboard() {
  // returns ok(frozen contract with:)
  // emit(eventName, payload) -> undefined
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conductor service tests (RED phase)</name>
  <files>
    core/services/conductor/__tests__/conductor.test.js
  </files>
  <read_first>
    core/services/forge/__tests__/forge.test.js,
    core/services/magnet/__tests__/magnet.test.js,
    core/services/lathe/__tests__/lathe.test.js
  </read_first>
  <behavior>
    Service lifecycle:
    - Test: createConductor() returns Ok(frozen conductor instance)
    - Test: conductor has all CONDUCTOR_SHAPE required methods (init, start, stop, healthCheck, composeUp, composeDown, composeStatus, checkDependencies, isDockerAvailable)
    - Test: init({ switchboard }) initializes successfully (no required deps beyond switchboard)
    - Test: healthCheck() returns { healthy: true, name: 'conductor' } after start()
    - Test: healthCheck() includes dockerAvailable: boolean

    Docker availability (per D-07):
    - Test: isDockerAvailable() returns true when docker is installed
    - Test: isDockerAvailable() returns false when docker is not found (mock Bun.spawnSync)

    Docker Compose lifecycle (per D-06):
    - Test: composeUp(composePath) runs `docker compose -f <path> up -d` and returns Ok
    - Test: composeUp when Docker not available returns Err('DOCKER_UNAVAILABLE')
    - Test: composeDown(composePath) runs `docker compose -f <path> down` and returns Ok
    - Test: composeDown when Docker not available returns Err('DOCKER_UNAVAILABLE')
    - Test: composeStatus(composePath) runs `docker compose -f <path> ps --format json` and returns parsed service status
    - Test: composeStatus when Docker not available returns Err('DOCKER_UNAVAILABLE')

    Dependency health checks:
    - Test: checkDependencies() returns Ok({ bun: { available, version }, git: { available, version }, duckdb: { loadable }, disk: { available, freeBytes } })
    - Test: checkDependencies().bun.version matches current Bun version
    - Test: checkDependencies().git.available is true (git is installed in test env)

    Event emission:
    - Test: composeUp emits 'infra:compose-up' via Switchboard
    - Test: composeDown emits 'infra:compose-down' via Switchboard
  </behavior>
  <action>
    Create `core/services/conductor/__tests__/conductor.test.js`:
    - `'use strict'`
    - Import `{ describe, test, expect, beforeEach }` from `bun:test`
    - Import `{ createConductor }` from `../conductor.cjs`
    - Import `{ unwrap }` from `../../../../lib/index.cjs`

    Test setup:
    - Create mock switchboard: `{ emit: (name, payload) => { calls.push({ name, payload }) } }`
    - Create conductor instance: `unwrap(createConductor())`
    - Initialize: `conductor.init({ switchboard: mockSwitchboard })`
    - Start: `conductor.start()`

    For Docker tests:
    - The actual Docker tests (composeUp/Down/Status) should check `isDockerAvailable()` first. If Docker is not available in the test environment, skip those tests with a console.warn (do not fail the build if Docker is absent).
    - The isDockerAvailable test should work in all environments (just checks `which docker`).
    - The dependency check tests should work universally since they check Bun and git (both always available).

    For graceful degradation tests:
    - Create a conductor instance that mocks Docker as unavailable (inject `_dockerAvailable = false` via options or test the behavior when `which docker` returns failure)
    - Verify composeUp/Down/Status return Err('DOCKER_UNAVAILABLE')

    Write all tests RED -- they will fail because conductor.cjs does not exist yet.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/dynamo" && bun test core/services/conductor/__tests__/conductor.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - core/services/conductor/__tests__/conductor.test.js exists
    - conductor.test.js imports from `../conductor.cjs`
    - conductor.test.js contains `describe('Conductor'` or `describe('createConductor'`
    - conductor.test.js contains tests for: composeUp, composeDown, composeStatus, checkDependencies, isDockerAvailable
    - conductor.test.js has graceful degradation tests (Docker unavailable returns Err)
    - conductor.test.js has mock switchboard for event emission tests
    - Tests fail (RED phase)
  </acceptance_criteria>
  <done>Conductor test suite written with coverage of Docker Compose lifecycle, dependency health checks, graceful degradation, and event emission. All tests fail (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Conductor service implementation (GREEN phase)</name>
  <files>
    core/services/conductor/conductor.cjs
  </files>
  <read_first>
    core/services/conductor/__tests__/conductor.test.js,
    core/services/forge/forge.cjs,
    core/services/switchboard/switchboard.cjs
  </read_first>
  <action>
    Create `core/services/conductor/conductor.cjs`:
    - `'use strict'`
    - Import `{ ok, err, createContract }` from `../../../lib/index.cjs`
    - Import `fs` from `node:fs`

    Define CONDUCTOR_SHAPE:
    ```javascript
    const CONDUCTOR_SHAPE = {
      required: ['init', 'start', 'stop', 'healthCheck', 'composeUp', 'composeDown', 'composeStatus', 'checkDependencies', 'isDockerAvailable'],
      optional: ['composeRestart', 'composeLogs']
    };
    ```

    `createConductor()` factory function:
    - Private state: `_started = false`, `_switchboard = null`, `_dockerAvailable = false`

    **Internal helper `_runDocker(args)`:**
    ```javascript
    function _runDocker(args) {
      if (!_dockerAvailable) {
        return err('DOCKER_UNAVAILABLE', 'Docker is not installed or not running');
      }
      const result = Bun.spawnSync(['docker', ...args], {
        env: process.env,
        stdin: 'ignore',
      });
      if (!result.success) {
        const stderr = result.stderr.toString().trim();
        return err('DOCKER_FAILED', `docker ${args[0]} failed: ${stderr}`, {
          command: ['docker', ...args],
          exitCode: result.exitCode,
        });
      }
      return ok(result.stdout.toString().trim());
    }
    ```

    **Lifecycle methods:**
    - `init(options)`: Store `_switchboard = options.switchboard || null`. Check Docker availability: run `Bun.spawnSync(['which', 'docker'], { stdin: 'ignore' })`. Set `_dockerAvailable = result.success`. If `options._dockerAvailable !== undefined`, use that override (for testing). Return ok(undefined).
    - `start()`: `_started = true`, return ok(undefined)
    - `stop()`: `_started = false`, return ok(undefined)
    - `healthCheck()`: Return ok({ healthy: _started, name: 'conductor', dockerAvailable: _dockerAvailable })

    **Docker Compose lifecycle (per D-06):**
    - `composeUp(composePath)`: Run `_runDocker(['compose', '-f', composePath, 'up', '-d'])`. If ok, emit 'infra:compose-up' via switchboard: `{ composePath }`. Return result.
    - `composeDown(composePath)`: Run `_runDocker(['compose', '-f', composePath, 'down'])`. If ok, emit 'infra:compose-down' via switchboard: `{ composePath }`. Return result.
    - `composeStatus(composePath)`: Run `_runDocker(['compose', '-f', composePath, 'ps', '--format', 'json'])`. Parse JSON output (each line is a JSON object for one service). Return ok(array of service status objects with { name, state, status, ports }).

    **Dependency health checks:**
    - `checkDependencies()`: Check each dependency and return aggregated result:
      1. **Bun**: `{ available: true, version: Bun.version }` (always available -- we're running on it)
      2. **git**: Run `Bun.spawnSync(['git', '--version'], { stdin: 'ignore' })`. Parse version from stdout (`git version X.Y.Z`). Return `{ available: result.success, version: parsed }`.
      3. **DuckDB**: Try `require('@duckdb/node-api')`. Return `{ loadable: true }` if require succeeds, `{ loadable: false, error: message }` if it throws.
      4. **disk**: Use `fs.statfsSync('/')` if available, or `Bun.spawnSync(['df', '-k', '/'])` and parse. Return `{ available: true, freeBytes: freeSpace }`. If check fails, return `{ available: false }`.
      5. **Docker**: `{ available: _dockerAvailable, version: dockerVersion }` (get version from `docker --version` if available)
      - Return ok({ bun, git, duckdb, disk, docker })

    **Graceful degradation (per D-07):**
    - All compose methods check `_dockerAvailable` first via `_runDocker` which returns err('DOCKER_UNAVAILABLE') if Docker is not present
    - healthCheck includes `dockerAvailable` field so consumers know
    - checkDependencies includes Docker status so consumers know

    **isDockerAvailable():**
    - Return ok(_dockerAvailable)

    Return `createContract('conductor', CONDUCTOR_SHAPE, impl)` where impl includes all methods.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/dynamo" && bun test core/services/conductor/__tests__/conductor.test.js</automated>
  </verify>
  <acceptance_criteria>
    - core/services/conductor/conductor.cjs exports `createConductor`
    - core/services/conductor/conductor.cjs contains `createContract('conductor'`
    - core/services/conductor/conductor.cjs contains `Bun.spawnSync` for Docker CLI execution
    - core/services/conductor/conductor.cjs contains `which docker` check for availability
    - core/services/conductor/conductor.cjs contains `DOCKER_UNAVAILABLE` error code for graceful degradation
    - core/services/conductor/conductor.cjs contains `composeUp`, `composeDown`, `composeStatus` methods
    - core/services/conductor/conductor.cjs contains `checkDependencies` that checks bun, git, duckdb, disk, docker
    - All conductor tests pass with `bun test core/services/conductor/`
  </acceptance_criteria>
  <done>Conductor service manages Docker Compose lifecycle (up/down/status), checks platform dependency health, and gracefully degrades when Docker is absent. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun test core/services/conductor/` -- all Conductor tests pass
- `bun test` -- full suite passes, no regressions
- Conductor reports Docker availability correctly
- Compose operations return DOCKER_UNAVAILABLE when Docker is absent
- Dependency checks report Bun, git, DuckDB, disk, Docker status
</verification>

<success_criteria>
- Conductor manages Docker Compose lifecycle for containerized services
- Conductor checks all platform dependency health status
- Conductor gracefully degrades when Docker is not installed
- All tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-providers-infrastructure-services/03-04-SUMMARY.md`
</output>
