---
phase: 01-methodology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-methodology/VETTING-PROTOCOL.md
  - .planning/phases/01-methodology/ANTI-FEATURES.md
autonomous: true
requirements:
  - INFR-01
  - INFR-02

must_haves:
  truths:
    - "A Phase 2 assessor can evaluate any MCP by applying only the vetting protocol gates, with no judgment calls on hard gates"
    - "A Phase 2 researcher can check whether a tool is pre-excluded by looking up the anti-features named list or applying category rules"
    - "The scorecard template can be copy-pasted and filled in for any candidate tool — every field has an explicit instruction for how to complete it"
    - "Recommendation tiers (INCLUDE/CONSIDER/DEFER) have pre-defined conditions so Phase 2 assessors assign tiers at assessment time"
  artifacts:
    - path: ".planning/phases/01-methodology/VETTING-PROTOCOL.md"
      provides: "Decision-tree gate structure, assessment scorecard template, tier criteria"
      contains: "Gate 1"
    - path: ".planning/phases/01-methodology/ANTI-FEATURES.md"
      provides: "Named exclusion list with justifications, category rules for unlisted tools, not-evaluated section"
      contains: "Category Rules"
  key_links:
    - from: ".planning/phases/01-methodology/VETTING-PROTOCOL.md"
      to: "Phase 2 tool assessments"
      via: "Gate decision tree + scorecard template"
      pattern: "Gate [1-4].*PASS.*FAIL"
    - from: ".planning/phases/01-methodology/ANTI-FEATURES.md"
      to: "Phase 2 pre-filter check"
      via: "Named list lookup + category rule evaluation"
      pattern: "Category.*CC built-in duplication"
---

<objective>
Write the two Phase 1 deliverables — VETTING-PROTOCOL.md and ANTI-FEATURES.md — that all Phase 2 tool assessments will be evaluated against. Both documents are authored from existing research and locked CONTEXT.md decisions, requiring no new investigation.

Purpose: Establish objective, binary criteria so Phase 2 assessors apply consistent standards to every candidate tool with no judgment calls on hard gates.
Output: Two markdown files in `.planning/phases/01-methodology/`
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-methodology/01-CONTEXT.md
@.planning/phases/01-methodology/01-RESEARCH.md
@.planning/research/FEATURES.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write VETTING-PROTOCOL.md — decision-tree gates, scorecard template, and tier criteria</name>
  <files>.planning/phases/01-methodology/VETTING-PROTOCOL.md</files>
  <read_first>
    - .planning/phases/01-methodology/01-CONTEXT.md (locked decisions — thresholds, gates, assessment template format, tier model)
    - .planning/phases/01-methodology/01-RESEARCH.md (pattern recommendations — decision tree structure, tier pre-definition criteria, anti-patterns to avoid)
    - .planning/research/FEATURES.md (star counts, commit dates, tool viability matrix — calibration evidence for thresholds)
    - .planning/research/SUMMARY.md (CC built-in tool list for duplication gate definition, recommended stack for tier examples)
    - .planning/research/PITFALLS.md (self-management failure modes for Gate 3 definition, security procedure for scorecard section)
  </read_first>
  <action>
Create `.planning/phases/01-methodology/VETTING-PROTOCOL.md` with the following exact structure and content:

**Document header:**
- Title: "Vetting Protocol: Claude Code Global Setup Enhancers"
- Effective date, version 1.0
- Purpose statement: "This protocol defines the binary gates, assessment scorecard, and recommendation tier criteria applied to every MCP/tool candidate in Phase 2. Hard gates are pass/fail with no judgment calls. Recommendation tiers have pre-defined conditions."

**Section 1: Decision Tree — Hard Gates**

Write as a numbered decision tree (not prose). Each gate is a binary yes/no with exact thresholds. Format each gate as:

```
### Gate 1: GitHub Stars (Tiered by Publisher)

**Question:** Does the tool's GitHub repo meet the minimum star threshold for its publisher type?

| Publisher Type | Examples | Minimum Stars |
|----------------|----------|---------------|
| Official vendor | GitHub, Microsoft, Anthropic, Brave, WordPress | 100 |
| Established org | Upstash, Firecrawl | 500 |
| Community/individual | Any repo not from a recognized vendor or established org | 1,000 |

**PASS:** Stars >= threshold for publisher type -> Proceed to Gate 2
**FAIL:** Stars < threshold -> Record "Stars: [count] below [tier] threshold of [required]" -> ELIMINATED
```

Gate 2: Commit Recency (Graduated Window)
- Question: "When was the last commit to the repository's default branch?"
- Thresholds: <=30 days = PREFERRED (proceed), 31-90 days = ACCEPTABLE with mandatory justification note (proceed with flag, note must state why acceptable e.g. "stable release, no open security issues"), >90 days = HARD FAIL -> ELIMINATED
- FAIL record format: "Last commit [YYYY-MM-DD] — [N] days ago — hard fail (>90 days)"
- ACCEPTABLE record format: "Last commit [YYYY-MM-DD] — [N] days ago — ACCEPTABLE: [justification]"

Gate 3: Self-Management Capability (4 Operations)
- Question: "Can Claude Code perform ALL four lifecycle operations via documented commands?"
- Four operations listed as separate rows: Install, Configure, Update, Troubleshoot
- Each operation must have a documented command from official sources
- PASS: All 4 ops have documented commands -> Proceed to Gate 4
- FAIL: Any op missing -> Record "Self-management FAIL: [op name(s)] not documented" -> ELIMINATED
- Include this exact note: "Self-management is PASSED only when all four operations have documented commands from official sources. Partial documentation = FAIL."

Gate 4: CC Built-in Duplication Check
- Question: "Does this tool duplicate a capability Claude Code provides natively?"
- Include the CC built-in capability table from FEATURES.md:

| CC Built-in Capability | Native Tool | Duplicating MCP (examples) |
|------------------------|-------------|---------------------------|
| File read/write/edit | Read, Write, Edit | Filesystem MCP |
| File search by pattern | Glob | Filesystem MCP |
| Content search | Grep | Filesystem MCP |
| Shell execution | Bash | Desktop Commander |
| Web page fetching | WebFetch | Fetch MCP |
| Web search | WebSearch | (evaluate carefully) |
| Subagent spawning | Task | — |
| Task tracking | TodoWrite | — |

- PASS: No duplication -> Proceed to scorecard
- FAIL: Duplicates a built-in -> Record "CC duplication: duplicates [tool name]" -> ELIMINATED
- Include note: "Tools that enhance or extend a CC built-in (e.g., Brave Search providing a different search index than WebSearch) are NOT duplicates. Only tools that replicate the same capability with no additional value are eliminated."

**Section 2: Assessment Scorecard Template**

Write as a copy-paste-ready markdown template with these exact sections (sourced from RESEARCH.md Code Examples section):

1. Identity table: Name, Repo URL, Stars (with date), Last Commit, Transport Type (stdio/http/sse), Publisher type (vendor-official/established-org/community)
2. Hard Gate Results table: 7 rows (Stars, Commit Recency, Self-Management Install, Self-Management Configure, Self-Management Update, Self-Management Troubleshoot, CC Duplication) — each with Threshold, Actual, Result columns
3. Gate Summary line: "ALL PASS -> continue to scorecard" or "FAILED: Gate N — reason -> ELIMINATED"
4. Context Cost Estimate table: Tool count exposed, Estimated token overhead, Source (how measured)
5. Self-Management Commands table: 4 rows (Install, Configure, Update, Troubleshoot) with Command and Source columns
6. Security Findings section: mcp-scan result, Known CVEs, Risk level (LOW/MEDIUM/HIGH), Notes. Include note: "Security is informational only — not a hard gate. Findings are presented in the Phase 3 ranked report for user decision."
7. WordPress/PHP Relevance section (optional): Include note "Complete if tool has PHP/WP-specific value; omit if general-purpose with no stack-specific considerations."
8. Pros and Cons section: Bulleted strengths and weaknesses/caveats
9. Verdict section: Tier (INCLUDE/CONSIDER/DEFER/ELIMINATED) + Rationale (1-2 sentences applying tier criteria)

Add a note above the template: "Copy this template for each Phase 2 assessment. Every structured field must be answerable by: (a) running a CLI command, (b) visiting a single specific URL, or (c) applying a rule from this protocol. If completing a field requires judgment or open-ended research, it belongs in Pros/Cons, not in a structured field."

**Section 3: Recommendation Tier Criteria**

Pre-define the three tiers with explicit conditions (from RESEARCH.md Pattern 3):

| Tier | Conditions |
|------|-----------|
| INCLUDE | Passes all 4 hard gates + fills a capability gap CC doesn't cover natively + no known overlap with another INCLUDE candidate |
| CONSIDER | Passes all 4 hard gates + provides value but overlaps with another INCLUDE candidate OR requires API key with potential cost implications OR scope is partially project-level rather than purely global |
| DEFER | Passes all 4 hard gates + genuinely valuable but has a timing dependency (e.g., awaiting WP 7.0), OR is a v2 requirement per REQUIREMENTS.md, OR adds value only after v1 tools are validated |

Include note: "Tier assignment is made at assessment time by the Phase 2 assessor, applying these conditions. If an edge case arises that doesn't cleanly match a tier, the assessor records the ambiguity in Pros/Cons and assigns the closest matching tier with an explanatory note."

**Section 4: SSE Transport Deprecation Notice**

One paragraph: "Tools using SSE (Server-Sent Events) transport are NOT automatically disqualified. SSE is flagged as a deprecation risk in the Identity section of the assessment scorecard. The risk is documented for the user's decision in the Phase 3 ranked report. Assessors should note whether the tool has an alternative transport (HTTP or stdio) available."

**Section 5: Community Fork Policy**

One paragraph: "Community forks of official vendor servers are assessed case-by-case — they are not blanket-excluded. A fork that adds genuine value over the official server is assessed on its own merits against all four gates. However, when an official vendor server exists and meets all gates, a community fork must demonstrate clear, documented superiority (not just additional features) to be preferred over the official server."
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-methodology/VETTING-PROTOCOL.md" && grep -c "^### Gate" ".planning/phases/01-methodology/VETTING-PROTOCOL.md" && grep -E "Install|Configure|Update|Troubleshoot" ".planning/phases/01-methodology/VETTING-PROTOCOL.md" | wc -l && grep -c "INCLUDE\|CONSIDER\|DEFER" ".planning/phases/01-methodology/VETTING-PROTOCOL.md"</automated>
  </verify>
  <acceptance_criteria>
    - VETTING-PROTOCOL.md exists at `.planning/phases/01-methodology/VETTING-PROTOCOL.md`
    - File contains exactly 4 gates: `grep -c "^### Gate" VETTING-PROTOCOL.md` returns 4
    - Gate 1 contains tiered star thresholds: `grep "100" VETTING-PROTOCOL.md` and `grep "500" VETTING-PROTOCOL.md` and `grep "1,000" VETTING-PROTOCOL.md` all return matches
    - Gate 2 contains graduated recency: `grep "30" VETTING-PROTOCOL.md` and `grep "90" VETTING-PROTOCOL.md` return matches
    - Gate 3 lists all 4 self-management ops as separate items: `grep -c -E "Install|Configure|Update|Troubleshoot" VETTING-PROTOCOL.md` returns >= 8 (appears in gate definition + scorecard template)
    - Gate 4 contains the CC built-in capability table: `grep "Filesystem MCP" VETTING-PROTOCOL.md` returns a match
    - Scorecard template contains all mandatory sections: `grep "Identity" VETTING-PROTOCOL.md` and `grep "Hard Gate Results" VETTING-PROTOCOL.md` and `grep "Context Cost" VETTING-PROTOCOL.md` and `grep "Security Findings" VETTING-PROTOCOL.md` and `grep "Verdict" VETTING-PROTOCOL.md` all return matches
    - Tier criteria table exists: `grep "INCLUDE.*fills a capability gap" VETTING-PROTOCOL.md` returns a match
    - Security is informational note: `grep "informational.*not a hard gate" VETTING-PROTOCOL.md` returns a match
    - SSE deprecation section: `grep -i "SSE.*not.*disqualified" VETTING-PROTOCOL.md` returns a match
    - Community fork policy: `grep "case-by-case" VETTING-PROTOCOL.md` returns a match
  </acceptance_criteria>
  <done>VETTING-PROTOCOL.md exists with 4 binary hard gates (stars tiered by publisher, commit recency graduated, self-management 4 ops, CC duplication), a copy-paste scorecard template with all mandatory sections, pre-defined INCLUDE/CONSIDER/DEFER tier criteria, SSE deprecation notice, and community fork policy. A Phase 2 assessor can evaluate any tool by following the decision tree and filling in the scorecard without making judgment calls on hard gates.</done>
</task>

<task type="auto">
  <name>Task 2: Write ANTI-FEATURES.md — named exclusion list, category rules, and not-evaluated section</name>
  <files>.planning/phases/01-methodology/ANTI-FEATURES.md</files>
  <read_first>
    - .planning/phases/01-methodology/01-CONTEXT.md (locked decisions — anti-feature categories, justification format 2-3 sentences, community fork policy, named list + category rules structure)
    - .planning/phases/01-methodology/01-RESEARCH.md (two-part document pattern, "Not Evaluated" escape hatch recommendation, justification format template)
    - .planning/research/FEATURES.md (Anti-Features table — named tools with "Why It Looks Good" and "Why to Avoid" columns; also the Baseline CC native tools table for duplication references)
    - .planning/research/SUMMARY.md (explicit anti-features list in "Expected Features" section; CC built-in capability list)
    - .planning/research/PITFALLS.md (Pitfall 2 tool poisoning — informs security/supply-chain category; Pitfall 1 abandoned MCPs — informs abandoned category)
    - .planning/REQUIREMENTS.md (out-of-scope items for the "Not Evaluated" section)
  </read_first>
  <action>
Create `.planning/phases/01-methodology/ANTI-FEATURES.md` with the following exact structure and content:

**Document header:**
- Title: "Anti-Features List: Claude Code Global Setup Enhancers"
- Effective date, version 1.0
- Purpose statement: "This document names every currently-known tool that should NOT be installed, with explicit justification for each exclusion. It also defines category rules so Phase 2 researchers can evaluate tools not yet on the named list. Tools that are simply outside this project's scope appear in the 'Not Evaluated' section — they are not anti-features."

**Part 1: Named Exclusion List**

Write each entry using the CONTEXT.md-mandated 3-sentence justification format. Source all entries from FEATURES.md Anti-Features table. The entries are:

1. **Filesystem MCP Server** (modelcontextprotocol/servers)
   - Category: CC built-in duplication
   - What: Provides granular file read/write/search capabilities with configurable access permissions.
   - Appeal: Appears in official MCP reference servers and looks like a must-have for any AI coding setup.
   - Exclusion: Completely duplicates Claude Code's native Read, Write, Edit, Glob, and Grep tools while adding ~10K+ context tokens of overhead — use CC's built-in file tools instead.

2. **Fetch MCP** (modelcontextprotocol/servers)
   - Category: CC built-in duplication
   - What: Fetches web page content and converts it to markdown for AI consumption.
   - Appeal: Official Anthropic reference server with the credibility of the MCP organization behind it.
   - Exclusion: Duplicates CC's native WebFetch tool and introduces a security concern (can access local/internal IPs) — use CC's built-in WebFetch instead.

3. **Memory MCP Server** (modelcontextprotocol/servers)
   - Category: Out-of-scope (already solved)
   - What: Provides persistent memory storage using a knowledge graph for context retention across sessions.
   - Appeal: Free, official reference implementation from Anthropic with broad community adoption.
   - Exclusion: Already solved with Graphiti (richer temporal knowledge graph with scoped groups, entity edges, and episode tracking) — running two memory systems creates storage conflicts and query ambiguity.

4. **Desktop Commander MCP** (wonderwhy-er/DesktopCommanderMCP)
   - Category: CC built-in duplication
   - What: Provides terminal command execution, file system operations, and process management with diff-based editing.
   - Appeal: 5,705 stars and designed for full system control including long-running commands and process management.
   - Exclusion: Designed for Claude Desktop (GUI app), not Claude Code (terminal) — CC already has native Bash for shell execution and native file tools for all filesystem operations, making Desktop Commander entirely redundant.

5. **PHPocalypse MCP** (plapinski/PHPocalypse-MCP)
   - Category: Abandoned/archived
   - What: Bundles PHPStan static analysis, PHP-CS-Fixer formatting, and PHPUnit testing into a single MCP for PHP code quality.
   - Appeal: All-in-one PHP quality tooling accessible via natural language commands.
   - Exclusion: Archived repository with only 3 stars and last commit March 2025 (12+ months stale) — call `vendor/bin/phpstan`, `vendor/bin/phpcs`, and `vendor/bin/phpunit` directly via CC's native Bash tool instead.

6. **lunetics/php-mcp-phpstan**
   - Category: Abandoned/archived
   - What: Purpose-built MCP server for running PHPStan static analysis within Claude Code conversations.
   - Appeal: Targeted PHP static analysis integration that could streamline code quality workflows.
   - Exclusion: 0 GitHub stars and last pushed July 2025 (8+ months stale), failing both the stars gate and the recency gate — run `vendor/bin/phpstan` directly via CC's Bash tool.

7. **Generic community MCP forks** (various)
   - Category: Security/supply chain risk
   - What: Community-maintained forks of official MCP servers that add features, fix bugs, or extend functionality beyond the original.
   - Appeal: Often offer more features or faster bug fixes than the official server they forked from.
   - Exclusion: Unverified authorship creates supply chain attack surface — tool descriptions can be silently amended for rug-pull attacks, and community forks lack the accountability of vendor-maintained servers. Use official org-owned repos; forks are assessed case-by-case only when no official alternative meets all gates (see Community Fork Policy in VETTING-PROTOCOL.md).

**Part 2: Category Rules**

Define the four categories so Phase 2 researchers can classify unlisted tools without updating the named list:

**Category 1: CC Built-in Duplication (Auto-disqualify)**
- Rule: If the tool replicates a capability from the CC built-in tools table (see Gate 4 in VETTING-PROTOCOL.md) with no additional value beyond what CC provides natively, it is an anti-feature.
- Action: Do not assess. Record: "Anti-feature — CC duplication: replicates [CC tool name]."
- Note: Tools that enhance or extend a CC built-in (e.g., providing a different search index) are NOT duplicates and should proceed through the full gate evaluation.

**Category 2: Abandoned/Archived Projects (Hard fail on recency gate)**
- Rule: If the tool's GitHub repository is archived, has 0 stars, or has not had a commit in over 90 days at assessment time, it is an anti-feature.
- Action: Do not assess. Record: "Anti-feature — Abandoned: last commit [date], [stars] stars."
- Note: This category catches tools that would fail Gate 2 (recency) anyway. The anti-features list enables O(1) lookup so assessors don't waste time running the full gate evaluation.

**Category 3: Out-of-Scope for This Project**
- Rule: If the tool serves a domain explicitly listed in PROJECT.md "Out of Scope" or REQUIREMENTS.md "Out of Scope", it is an anti-feature.
- Action: Move to "Not Evaluated" section (see Part 3) rather than listing here, UNLESS the tool is commonly recommended alongside in-scope tools and would create scope creep if not explicitly flagged. In that case, list as a named anti-feature with justification.
- Note: The distinction matters — anti-features are tools that look appealing but have a concrete disqualifying reason. Out-of-scope tools are simply not relevant.

**Category 4: Security/Supply Chain Risk**
- Rule: If the tool is distributed only as a binary with no source code, requires permissions far beyond its stated purpose, has been flagged by mcp-scan for tool description manipulation, or exhibits typosquatting patterns (npm package name closely resembles a known tool from a different publisher), it is an anti-feature.
- Action: Record: "Anti-feature — Security risk: [specific concern]." Do not install or recommend.
- Note: This category is distinct from the security findings in the assessment scorecard. Scorecard security findings are informational and not a hard gate. Category 4 anti-features are tools where the security concern is so fundamental (no source code, typosquatting) that assessment is not warranted.

**Part 3: Not Evaluated**

List tools that are outside the project's scope entirely. These are NOT anti-features — they simply weren't candidates. Purpose: prevent scope creep by making "not evaluated" an explicit category that signals "don't investigate this."

Entries (sourced from REQUIREMENTS.md Out of Scope + FEATURES.md):

| Tool | Domain | Why Not Evaluated |
|------|--------|-------------------|
| Notion MCP | Project management | Out of scope — no PM integration requested; ~17K tokens per server |
| Jira MCP | Project management | Out of scope — no PM integration requested; ~17K tokens per server |
| Atlassian MCP | Project management | Out of scope — no PM integration requested; ~17K tokens per server |
| Database/SQL MCPs (PostgreSQL, SQLite, etc.) | Database access | Out of scope per PROJECT.md — not requested; DDEV provides direct DB access |
| SSE-only transport MCPs (no HTTP/stdio alternative) | Various | Deprecated protocol — SSE transport removed from MCP spec as of March 2026; tools with no alternative transport are not evaluated |

Add closing note: "If a Phase 2 researcher encounters a tool not on the named exclusion list and not in the 'Not Evaluated' section, apply the category rules above. If the tool doesn't match any category, proceed with the full gate evaluation in VETTING-PROTOCOL.md."
  </action>
  <verify>
    <automated>test -f ".planning/phases/01-methodology/ANTI-FEATURES.md" && grep -c "^### \|^[0-9]\." ".planning/phases/01-methodology/ANTI-FEATURES.md" && grep -c "Category" ".planning/phases/01-methodology/ANTI-FEATURES.md" && grep "Not Evaluated" ".planning/phases/01-methodology/ANTI-FEATURES.md" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - ANTI-FEATURES.md exists at `.planning/phases/01-methodology/ANTI-FEATURES.md`
    - Named exclusion list contains all 7 entries: `grep -c "Filesystem MCP\|Fetch MCP\|Memory MCP\|Desktop Commander\|PHPocalypse\|lunetics\|Generic community" ANTI-FEATURES.md` returns 7
    - Each named entry has all 3 justification components: `grep -c "What:\|Appeal:\|Exclusion:" ANTI-FEATURES.md` returns >= 21 (3 per entry x 7 entries) OR each entry uses the heading format with 3 sentences in the body
    - Category rules section exists with 4 categories: `grep -c "Category [1-4]" ANTI-FEATURES.md` returns 4
    - Category 1 references CC built-in duplication: `grep "CC Built-in Duplication" ANTI-FEATURES.md` returns a match
    - Category 2 references abandoned/archived: `grep "Abandoned" ANTI-FEATURES.md` returns a match
    - Category 3 references out-of-scope: `grep "Out-of-Scope" ANTI-FEATURES.md` returns a match
    - Category 4 references security/supply chain: `grep "Security.*Supply Chain" ANTI-FEATURES.md` returns a match
    - "Not Evaluated" section exists: `grep "Not Evaluated" ANTI-FEATURES.md` returns a match
    - Not Evaluated section contains the out-of-scope tools: `grep "Notion\|Jira\|Atlassian\|Database" ANTI-FEATURES.md` returns matches
    - Closing note references VETTING-PROTOCOL.md: `grep "VETTING-PROTOCOL" ANTI-FEATURES.md` returns a match
  </acceptance_criteria>
  <done>ANTI-FEATURES.md exists with 7 named exclusion entries (each with 3-sentence justification), 4 category rules for classifying unlisted tools, and a separate "Not Evaluated" section for out-of-scope tools. A Phase 2 researcher can check whether any tool is pre-excluded in O(1) by searching the named list, then fall through to category rules for unlisted tools.</done>
</task>

</tasks>

<verification>
Phase 1 is complete when both deliverables exist and satisfy the ROADMAP success criteria:

1. **Documented vetting protocol exists** — VETTING-PROTOCOL.md has 4 programmatic hard gates (stars by tier, commit recency graduated, self-management 4 ops, CC duplication check) plus a scorecard template and tier criteria
2. **Anti-features list exists** — ANTI-FEATURES.md names every known excluded tool with explicit reasoning and defines category rules for future evaluation
3. **No judgment calls on hard gates** — Any tool assessment in Phase 2 can be completed by following the decision tree and filling in the scorecard template; judgment is confined to the Pros/Cons narrative section and tier assignment (which has pre-defined conditions)

Automated verification:
```bash
test -f ".planning/phases/01-methodology/VETTING-PROTOCOL.md" && \
test -f ".planning/phases/01-methodology/ANTI-FEATURES.md" && \
echo "Both deliverables exist" || echo "MISSING deliverables"

# Gate count check
grep -c "^### Gate" ".planning/phases/01-methodology/VETTING-PROTOCOL.md"
# Expected: 4

# Named anti-features count
grep -c "^[0-9]\. \*\*" ".planning/phases/01-methodology/ANTI-FEATURES.md"
# Expected: 7

# Category rules count
grep -c "Category [1-4]" ".planning/phases/01-methodology/ANTI-FEATURES.md"
# Expected: 4
```
</verification>

<success_criteria>
- VETTING-PROTOCOL.md exists with all 4 hard gates as binary pass/fail questions, a copy-paste scorecard template, and pre-defined tier criteria
- ANTI-FEATURES.md exists with 7 named exclusions (each with 3-component justification), 4 category rules, and a "Not Evaluated" section
- Both documents reference each other where appropriate (anti-features list references vetting protocol gates; vetting protocol references anti-features for pre-filter)
- A Phase 2 assessor reading only these two documents (plus PROJECT.md for context) can evaluate any candidate tool without asking clarifying questions
</success_criteria>

<output>
After completion, create `.planning/phases/01-methodology/01-01-SUMMARY.md`
</output>
