---
phase: 04-framework
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - core/armature/index.cjs
  - core/core.cjs
  - core/armature/__tests__/integration.test.js
autonomous: true
requirements: [FWK-01, FWK-02, FWK-03, FWK-04, FWK-05, FWK-06]

must_haves:
  truths:
    - "core/core.cjs bootstraps all 9 services and 2 providers into a running container"
    - "All services resolve through facades with domain metadata"
    - "Services receive their declared dependencies via init() options"
    - "container.resolve('providers.data.sql') returns the Ledger facade"
    - "container.resolve('providers.data.files') returns the Journal facade"
    - "Armature barrel export provides single import point for all framework APIs"
    - "Full bootstrap completes without errors when services are properly configured"
  artifacts:
    - path: "core/armature/index.cjs"
      provides: "Barrel export for all Armature modules"
      exports: ["createContainer", "createFacade", "createLifecycle", "createHookRegistry", "HOOK_SCHEMAS", "HOOK_EVENT_NAMES", "PLUGIN_MANIFEST_SCHEMA", "validateManifest", "checkDependencies", "loadPlugin", "discoverPlugins"]
    - path: "core/core.cjs"
      provides: "Bootstrap entry point that creates container, registers all, runs lifecycle"
      exports: ["bootstrap"]
    - path: "core/armature/__tests__/integration.test.js"
      provides: "Full integration test bootstrapping all 11 components"
  key_links:
    - from: "core/core.cjs"
      to: "core/armature/container.cjs"
      via: "createContainer for IoC setup"
      pattern: "createContainer"
    - from: "core/core.cjs"
      to: "core/armature/lifecycle.cjs"
      via: "createLifecycle for boot orchestration"
      pattern: "createLifecycle"
    - from: "core/core.cjs"
      to: "core/services/switchboard/switchboard.cjs"
      via: "require for service factory registration"
      pattern: "createSwitchboard"
    - from: "core/core.cjs"
      to: "core/providers/ledger/ledger.cjs"
      via: "require for provider factory registration"
      pattern: "createLedger"
    - from: "core/armature/index.cjs"
      to: "core/armature/container.cjs"
      via: "barrel re-export"
      pattern: "require\\('./container\\.cjs'\\)"
---

<objective>
Wire everything together: create the Armature barrel export, the core/core.cjs bootstrap entry point, and the full integration test. This plan validates that all Armature modules compose correctly to bring the entire Dynamo platform to life.

Purpose: core.cjs is the single entry point for the platform (per D-07). It creates a container, registers all 9 services and 2 providers with their dependency metadata and domain tags, runs the lifecycle, and exports the ready container. The integration test validates the full bootstrap path end-to-end.

Output: core/armature/index.cjs barrel, core/core.cjs bootstrap, integration test proving all 11 components boot correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-framework/04-CONTEXT.md
@.planning/phases/04-framework/04-RESEARCH.md

@core/armature/container.cjs
@core/armature/facade.cjs
@core/armature/hooks.cjs
@core/armature/plugin.cjs
@core/armature/lifecycle.cjs
@lib/index.cjs
@lib/config.cjs
@lib/paths.cjs

@.planning/phases/04-framework/04-01-SUMMARY.md
@.planning/phases/04-framework/04-02-SUMMARY.md
@.planning/phases/04-framework/04-03-SUMMARY.md

<interfaces>
From core/armature/container.cjs:
```javascript
module.exports = { createContainer };
// bind, singleton, factory, resolve, resolveTagged, has, getMetadata, getBootOrder, getRegistry
```

From core/armature/facade.cjs:
```javascript
module.exports = { createFacade };
// createFacade(name, contract, metadata) -> frozen facade with delegation, hooks, override
```

From core/armature/lifecycle.cjs:
```javascript
module.exports = { createLifecycle };
// register, boot, shutdown, getFacade, getStatus
```

From core/armature/hooks.cjs:
```javascript
module.exports = { HOOK_SCHEMAS, HOOK_EVENT_NAMES, createHookRegistry };
```

From core/armature/plugin.cjs:
```javascript
module.exports = { PLUGIN_MANIFEST_SCHEMA, validateManifest, checkDependencies, loadPlugin, discoverPlugins };
```

Service factories (all return Result via createContract):
```
createSwitchboard()  -- init({})
createLathe()        -- init({})
createCommutator()   -- init({ switchboard })
createMagnet()       -- async init({ switchboard?, provider? })
createConductor()    -- init({ switchboard? })
createForge()        -- init({ repoPath, lathe?, switchboard? })
createRelay()        -- init({ forge, lathe?, switchboard?, configPath? })
createWire()         -- init({ switchboard, conductor?, ledger?, ...config })
createAssay()        -- init({ switchboard?, ledger?, journal? })
createLedger()       -- init({ dbPath, switchboard?, backend? })
createJournal()      -- init({ basePath, lathe, switchboard? })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Armature Barrel Export and Bootstrap Entry Point (per D-07)</name>
  <files>core/armature/index.cjs, core/core.cjs</files>
  <read_first>
    - core/armature/container.cjs (exports to re-export)
    - core/armature/facade.cjs (exports to re-export)
    - core/armature/lifecycle.cjs (exports to re-export)
    - core/armature/hooks.cjs (exports to re-export)
    - core/armature/plugin.cjs (exports to re-export)
    - lib/index.cjs (barrel export pattern to replicate)
    - lib/config.cjs (loadConfig for bootstrap config loading)
    - lib/paths.cjs (discoverRoot/createPaths for path resolution)
    - core/services/forge/forge.cjs (init signature with required repoPath -- example of config-sourced dep)
    - core/providers/ledger/ledger.cjs (init signature with required dbPath)
    - core/providers/journal/journal.cjs (init signature with required basePath + lathe)
  </read_first>
  <action>
    Create two files:

    **core/armature/index.cjs** -- Barrel export. Requires each Armature module and re-exports all public APIs in a flat object. Follow the exact pattern of lib/index.cjs (section comments grouping by source module). Must export at least 11 named exports: createContainer, createFacade, createLifecycle, HOOK_SCHEMAS, HOOK_EVENT_NAMES, createHookRegistry, PLUGIN_MANIFEST_SCHEMA, validateManifest, checkDependencies, loadPlugin, discoverPlugins.

    **core/core.cjs** -- Bootstrap entry point. Contains a single async function `bootstrap(options = {})` that:

    1. Resolves paths: calls `discoverRoot()` then `createPaths(root)` from lib/paths.cjs. If `options.paths` is provided, use that instead (for test isolation with tmpdir).
    2. Loads config: calls `loadConfig()` from lib/config.cjs. If `options.configOverrides` provided, merge them.
    3. Creates container via `createContainer()`.
    4. Registers all 9 services as singletons. The exact registrations with their dependency metadata:

    - services.switchboard: deps=[], tags=['service','events']
    - services.lathe: deps=[], tags=['service','filesystem']
    - services.commutator: deps=['services.switchboard'], tags=['service','io'], mapDeps={'services.switchboard':'switchboard'}
    - services.magnet: deps=[], tags=['service','state'], mapDeps={'services.switchboard':'switchboard'}
    - services.conductor: deps=[], tags=['service','infrastructure'], mapDeps={'services.switchboard':'switchboard'}
    - services.forge: deps=['services.lathe'], tags=['service','git'], mapDeps={'services.lathe':'lathe','services.switchboard':'switchboard'}, config={repoPath: paths.root}
    - services.relay: deps=['services.forge','services.lathe'], tags=['service','operations'], mapDeps={'services.forge':'forge','services.lathe':'lathe','services.switchboard':'switchboard'}, config={configPath: paths.config}
    - services.wire: deps=['services.switchboard'], tags=['service','communication'], mapDeps={'services.switchboard':'switchboard','services.conductor':'conductor','providers.ledger':'ledger'}
    - services.assay: deps=[], tags=['service','search'], mapDeps={'services.switchboard':'switchboard','providers.ledger':'ledger','providers.journal':'journal'}

    5. Registers both providers with domain tags and aliases:

    - providers.ledger: deps=[], tags=['provider','data','sql'], aliases=['providers.data.sql'], config={dbPath: paths.root+'/data/ledger.db'}, mapDeps={'services.switchboard':'switchboard'}
    - providers.journal: deps=['services.lathe'], tags=['provider','data','files'], aliases=['providers.data.files'], mapDeps={'services.lathe':'lathe','services.switchboard':'switchboard'}, config={basePath: paths.root+'/data/journal'}

    6. Creates lifecycle: `createLifecycle(container, { pluginsDir: options.pluginsDir || paths.root + '/plugins' })`
    7. Calls `await lifecycle.boot()`
    8. Returns `ok({ container, lifecycle, config, paths })` on success, or the Err from boot on failure.

    Export: `module.exports = { bootstrap }`
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/dynamo" && node -e "const m=require('./core/armature/index.cjs');const k=Object.keys(m);console.log(k.length+' exports');if(k.length<10)process.exit(1)" && node -e "const m=require('./core/core.cjs');if(typeof m.bootstrap!=='function')process.exit(1);console.log('bootstrap is function')"</automated>
  </verify>
  <acceptance_criteria>
    - core/armature/index.cjs exists and contains 'use strict' on line 1
    - core/armature/index.cjs contains createContainer, createFacade, createLifecycle, HOOK_SCHEMAS, createHookRegistry
    - core/armature/index.cjs exports at least 11 named properties
    - core/core.cjs exists and contains 'use strict' on line 1
    - core/core.cjs contains `module.exports = { bootstrap }`
    - core/core.cjs contains `async function bootstrap(`
    - core/core.cjs contains `createContainer` (creates the IoC container)
    - core/core.cjs contains `createLifecycle` (creates the lifecycle orchestrator)
    - core/core.cjs contains all 9 service registration calls: services.switchboard, services.lathe, services.commutator, services.magnet, services.conductor, services.forge, services.relay, services.wire, services.assay
    - core/core.cjs contains both provider registrations: providers.ledger, providers.journal
    - core/core.cjs contains domain aliases: providers.data.sql, providers.data.files
    - core/core.cjs requires all 11 factory files (9 services + 2 providers)
    - Both files loadable via require() without errors
  </acceptance_criteria>
  <done>Armature barrel exports all framework APIs. Bootstrap registers all 11 components with correct dependency metadata, domain tags, and aliases. Both files require without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Full Integration Test</name>
  <files>core/armature/__tests__/integration.test.js</files>
  <read_first>
    - core/core.cjs (bootstrap function to call in test)
    - core/armature/lifecycle.cjs (lifecycle.getFacade, lifecycle.getStatus methods to verify)
    - core/armature/container.cjs (container.resolve, container.resolveTagged, container.has to verify)
    - core/armature/facade.cjs (facade.meta property to verify)
    - lib/paths.cjs (createPaths for providing test tmpdir paths)
  </read_first>
  <action>
    Create `core/armature/__tests__/integration.test.js` with an integration test that bootstraps the full platform using mock paths (tmpdir). The test must:

    1. Create a temp directory structure for test isolation:
       - `tmpDir/data/ledger.db` parent dirs
       - `tmpDir/data/journal/` directory
       - `tmpDir/config.json` with minimal valid config
       - `tmpDir/plugins/` empty directory

    2. Call `bootstrap({ paths: testPaths })` where testPaths mimics the output of createPaths() but pointing at tmpDir:
       ```javascript
       const testPaths = {
         root: tmpDir,
         lib: tmpDir + '/lib',
         core: tmpDir + '/core',
         services: tmpDir + '/core/services',
         providers: tmpDir + '/core/providers',
         plugins: tmpDir + '/plugins',
         modules: tmpDir + '/modules',
         extensions: tmpDir + '/extensions',
         config: tmpDir + '/config.json',
       };
       ```

    3. Verify the bootstrap result:
       - result.ok is true
       - result.value has container, lifecycle, config, paths properties
       - lifecycle.getStatus() returns 'running'

    4. Verify container resolution:
       - container.has('services.switchboard') is true
       - container.has('services.lathe') is true
       - container.has('providers.ledger') is true
       - container.has('providers.journal') is true
       - container.resolve('providers.data.sql') returns a facade (alias resolution)
       - container.resolve('providers.data.files') returns a facade (alias resolution)
       - container.resolveTagged('service') returns at least 9 entries
       - container.resolveTagged('provider') returns at least 2 entries
       - container.resolveTagged('sql') includes Ledger

    5. Verify facade metadata:
       - lifecycle.getFacade('services.switchboard').meta.name equals 'services.switchboard'
       - lifecycle.getFacade('providers.ledger').meta.tags includes 'sql'

    6. Verify shutdown:
       - lifecycle.shutdown() completes without error
       - lifecycle.getStatus() returns 'stopped'

    7. Cleanup: remove tmpDir in afterAll

    Note: Some services may return errors during init if they require real infrastructure (DuckDB for Ledger, git repo for Forge). The integration test should handle this gracefully -- test what CAN boot in isolation, skip assertions for services that need real infrastructure. Use `try/catch` or check for specific services that booted successfully.

    Alternative approach if full bootstrap is too fragile: Test with a subset of services that can boot without infrastructure (Switchboard, Lathe, Commutator, Magnet, Conductor, Assay) by using `options.skipServices` or by registering only those services manually in the test.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/dynamo" && bun test core/armature/__tests__/integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - core/armature/__tests__/integration.test.js exists
    - integration.test.js contains `require('bun:test')`
    - integration.test.js contains `bootstrap` (imports and calls the bootstrap function)
    - integration.test.js contains `services.switchboard` (verifies service registration)
    - integration.test.js contains `providers.data.sql` (verifies domain alias resolution)
    - integration.test.js contains `providers.data.files` (verifies domain alias resolution)
    - integration.test.js contains `resolveTagged` (verifies tag-based resolution)
    - integration.test.js contains `shutdown` (verifies clean shutdown)
    - bun test core/armature/__tests__/integration.test.js exits 0
  </acceptance_criteria>
  <done>Integration test proves bootstrap creates container with all registrations, resolves services by name/alias/tag, facades carry correct metadata, and shutdown completes cleanly. All tests pass.</done>
</task>

</tasks>

<verification>
- `bun test core/armature/__tests__/` -- all armature tests pass (container, facade, hooks, plugin, lifecycle, integration)
- `bun test lib/__tests__/` -- all lib tests pass (no regressions from schema enhancement)
- `bun test` -- full test suite green
</verification>

<success_criteria>
- Armature barrel provides single import for all framework APIs
- Bootstrap registers all 9 services and 2 providers with correct metadata
- Domain aliases (providers.data.sql, providers.data.files) resolve correctly
- Tagged resolution returns services/providers by domain category
- Full lifecycle (boot + run + shutdown) completes without errors
- Integration test validates the complete composition
</success_criteria>

<output>
After completion, create `.planning/phases/04-framework/04-04-SUMMARY.md`
</output>
