# Phase 12.1: Platform Launch Readiness - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-25
**Phase:** 12.1-platform-launch-readiness
**Areas discussed:** Skill design, Multi-triplet isolation, Visual session markers, E2E validation approach

---

## Skill Design

### What should /dynamo provide?

| Option | Description | Selected |
|--------|-------------|----------|
| Platform dashboard | Single /dynamo skill showing platform health, loaded modules, active services, hook status. Init/boot handled automatically. | ✓ |
| Init + status combined | /dynamo boots platform if not running, shows status if running. Dual-purpose entry point. | |
| Namespace for subcommands | /dynamo status, /dynamo health, /dynamo config — mirrors Pulley CLI as skills. | |

**User's choice:** Platform dashboard
**Notes:** None

### What should /reverie provide?

| Option | Description | Selected |
|--------|-------------|----------|
| Session management hub | Shows mode, topology, offers actions: start, status, inspect, stop. Interactive entry point. | ✓ |
| Start/stop only | Starts session if not running, stops if running. Lean entry point. | |
| Full subcommand mirror | /reverie status, /reverie inspect, etc. Mirrors Pulley CLI subcommands. | |

**User's choice:** Session management hub
**Notes:** None

### How should skills relate to Pulley CLI?

| Option | Description | Selected |
|--------|-------------|----------|
| Skills as conversational wrappers | Skills call same underlying Pulley logic, present results contextually. CLI stays for scripting. | ✓ |
| Skills replace CLI for humans | Skills become primary human interface. CLI still works but not promoted. | |
| Skills invoke CLI directly | Thin wrappers that run Pulley commands via Bash. | |

**User's choice:** Skills as conversational wrappers
**Notes:** None

### Auto-init behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-init on first skill use | Skill triggers bootstrap transparently if not booted. | |
| Require explicit init | User must /dynamo first before /reverie works. | |
| SessionStart handles it | Bootstrap via SessionStart hook automatically. | |

**User's choice:** Other — "Should be toggleable in dynamo config"
**Notes:** User wants the auto-init behavior to be configurable rather than picking a fixed default.

---

## Multi-Triplet Isolation

### How should triplets be identified in Wire?

| Option | Description | Selected |
|--------|-------------|----------|
| Triplet ID prefix | Each triplet gets unique ID. Session IDs: triplet-a1b2:primary, etc. Wire partitions by prefix. | ✓ |
| Session group registry | Wire maintains named session groups. Topology rules apply within groups. | |
| Separate Wire instances | Each triplet gets own Wire instance. Complete isolation, more resources. | |

**User's choice:** Triplet ID prefix
**Notes:** None

### How should Switchboard events be scoped?

| Option | Description | Selected |
|--------|-------------|----------|
| Triplet-prefixed events | Events carry triplet_id in payload. Listeners filter. Minimal Switchboard changes. | ✓ |
| Per-triplet event proxy | ScopedSwitchboard wrapper auto-filters per triplet. Subscribers never see cross-triplet events. | |
| Namespace partitioning | Event names include triplet: reverie:triplet-a1b2:formation:complete. | |

**User's choice:** Triplet-prefixed events
**Notes:** None

### Ledger concurrency model

| Option | Description | Selected |
|--------|-------------|----------|
| Single-writer stays | DuckDB single-writer stays. Write coordinator serializes. Triplet ID in rows. | ✓ |
| Triplet-scoped databases | Each triplet gets own DuckDB file. Complete isolation. | |
| You decide | Claude determines best approach. | |

**User's choice:** Single-writer stays
**Notes:** None

### Concurrent triplet limit

| Option | Description | Selected |
|--------|-------------|----------|
| Configurable cap, default 3 | Dynamo config max_triplets. Default 3 (9 total sessions). | ✓ |
| No hard limit | Users spawn as many as they want. Resources are natural limit. | |
| Fixed at 1 for v1 | Architecture built for multi, but limited to 1 in v1. | |

**User's choice:** Configurable cap, default 3
**Notes:** None

---

## Visual Session Markers

### How should users distinguish sessions?

| Option | Description | Selected |
|--------|-------------|----------|
| Terminal title + status line | Set terminal title + Claude Code status line badge. Lightweight. | ✓ |
| CLAUDE.md session header | Inject session role info into CLAUDE.md section. | |
| Color-coded output prefix | Prefix output with colored role tag: [Primary], [Mind], [Subconscious]. | ✓ |

**User's choice:** Both options 1 and 3 — terminal title + color-coded output prefix
**Notes:** User selected both for dual visual reinforcement.

### Color scheme

| Option | Description | Selected |
|--------|-------------|----------|
| Semantic colors | Blue [Face], Green [Mind], Purple [Subconscious]. | |
| Warm/cool spectrum | Gold, Cyan, Violet temperature gradient. | |
| You decide | Claude picks colors that work across terminal themes. | ✓ |

**User's choice:** You decide
**Notes:** Must work on both dark and light terminal backgrounds.

### Triplet ID visibility

| Option | Description | Selected |
|--------|-------------|----------|
| Short hash suffix | [Face a1b2], [Mind a1b2]. Users see which sessions belong together. | ✓ |
| Named triplets | Users name triplets: [Face 'work']. More readable, requires naming. | |
| No triplet ID | Just role: [Face], [Mind]. Simpler for single triplet. | |

**User's choice:** Short hash suffix
**Notes:** None

---

## E2E Validation Approach

### What does 'the platform runs' mean?

| Option | Description | Selected |
|--------|-------------|----------|
| Scripted live session | Validation script boots Dynamo in real Claude Code, verifies each success criterion. | |
| Integration test harness | bun:test suite mocking Claude Code hook contract. Fast, repeatable. | |
| Both layers | Integration tests + scripted live session. Defense in depth. | ✓ |

**User's choice:** Both — with emphasis that scripted session must boot Secondary and Tertiary automatically
**Notes:** "The scripted session needs to boot the secondary and tertiary sessions automatically just as would happen when the user invokes the reverie skill slash command to start a triplet session"

### How should results be reported?

| Option | Description | Selected |
|--------|-------------|----------|
| Checkpoint log file | Structured log with pass/fail per criterion, timestamps, evidence. | ✓ |
| Console output only | Prints to terminal, no persistent artifact. | |
| You decide | Claude picks. | |

**User's choice:** Checkpoint log file
**Notes:** None

### Should validation be a Claude skill?

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, as /dynamo validate | Skill runs full E2E validation and reports results. | ✓ |
| CLI only | Pulley CLI command, not a skill. | |
| Both | CLI for scripting, skill for conversation. | |

**User's choice:** Yes, as /dynamo validate
**Notes:** None

### Go-live gate criteria

| Option | Description | Selected |
|--------|-------------|----------|
| All 6 success criteria green | Every criterion must pass. Any red blocks go-live. Yellow acceptable with workaround. | ✓ |
| Core 4 required, 2 advisory | Boot + hooks + personality + formation required. Multi-triplet and lifecycle advisory. | |
| You decide | Claude determines blocking vs advisory. | |

**User's choice:** All 6 success criteria green
**Notes:** None

---

## Claude's Discretion

- Module discovery mechanism implementation details
- Auto-init default behavior (must be configurable, default is Claude's choice)
- Color scheme for session role prefixes
- Integration test harness structure
- Checkpoint log file format
- Skill .md file structure
- /dynamo validate orchestration approach

## Deferred Ideas

None — discussion stayed within phase scope.
