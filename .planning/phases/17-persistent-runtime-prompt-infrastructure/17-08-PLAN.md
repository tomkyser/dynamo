---
phase: 17-persistent-runtime-prompt-infrastructure
plan: 08
type: execute
wave: 4
depends_on: [17-06]
files_modified:
  - modules/reverie/prompts/framing-full.md
  - modules/reverie/prompts/framing-dual.md
  - modules/reverie/prompts/framing-soft.md
  - modules/reverie/prompts/rem-quality-eval.md
  - modules/reverie/prompts/rem-editorial.md
  - modules/reverie/prompts/sublimation-system.md
  - modules/reverie/prompts/face-prompt.md
  - modules/reverie/components/context/referential-framing.cjs
  - modules/reverie/components/rem/quality-evaluator.cjs
  - modules/reverie/components/rem/editorial-pass.cjs
  - modules/reverie/components/session/sublimation-loop.cjs
  - modules/reverie/components/context/context-manager.cjs
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - "All 7 remaining prompt string literals (3 framing, 2 REM, 1 sublimation, 1 face prompt) extracted to markdown templates"
    - "referential-framing.cjs loads framing templates via Linotype"
    - "quality-evaluator.cjs loads quality eval template via Linotype"
    - "editorial-pass.cjs loads editorial template via Linotype"
    - "sublimation-loop.cjs loads sublimation system prompt via Linotype"
    - "context-manager.cjs uses Linotype for face prompt assembly"
  artifacts:
    - path: "modules/reverie/prompts/framing-full.md"
      provides: "Full referential framing mode template"
      contains: "framing-full"
    - path: "modules/reverie/prompts/rem-editorial.md"
      provides: "REM editorial pass template (7 sections)"
      contains: "rem-editorial"
    - path: "modules/reverie/prompts/face-prompt.md"
      provides: "Face prompt assembly template"
      contains: "face-prompt"
    - path: "modules/reverie/prompts/sublimation-system.md"
      provides: "Tertiary sublimation system prompt"
      contains: "sublimation-system"
  key_links:
    - from: "modules/reverie/components/context/referential-framing.cjs"
      to: "modules/reverie/prompts/framing-*.md"
      via: "Linotype parse/cast"
      pattern: "require.*linotype"
    - from: "modules/reverie/components/context/context-manager.cjs"
      to: "modules/reverie/prompts/face-prompt.md"
      via: "Linotype parse/cast for face prompt assembly"
      pattern: "require.*linotype"
---

<objective>
Extract remaining prompt string literals (framing, REM, sublimation, face prompt) and update skills.

Purpose: This is the second prompt extraction plan, covering the 7 remaining prompts: 3 referential framing modes, quality evaluator, editorial pass, sublimation system prompt, and face prompt. Each source .cjs file is updated to load templates via Linotype instead of containing string literals. After this plan, ALL Reverie prompts are in modules/reverie/prompts/ -- zero string-literal prompts remain in .cjs files (per D-08).

Output: 7 more markdown template files (15 total), all source files updated to use Linotype.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-CONTEXT.md
@.claude/unfuck-the-whole-damn-thing.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-02-SUMMARY.md
@.planning/phases/17-persistent-runtime-prompt-infrastructure/17-06-SUMMARY.md
@modules/reverie/components/context/referential-framing.cjs
@modules/reverie/components/rem/quality-evaluator.cjs
@modules/reverie/components/rem/editorial-pass.cjs
@modules/reverie/components/session/sublimation-loop.cjs
@modules/reverie/components/context/context-manager.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create framing, REM, sublimation, and face prompt templates</name>
  <files>modules/reverie/prompts/framing-full.md, modules/reverie/prompts/framing-dual.md, modules/reverie/prompts/framing-soft.md, modules/reverie/prompts/rem-quality-eval.md, modules/reverie/prompts/rem-editorial.md, modules/reverie/prompts/sublimation-system.md, modules/reverie/prompts/face-prompt.md</files>
  <read_first>
    - modules/reverie/components/context/referential-framing.cjs (FULL read -- extract all 3 framing mode strings, lines ~32-57)
    - modules/reverie/components/rem/quality-evaluator.cjs (FULL read -- extract quality eval prompt, lines ~106-134)
    - modules/reverie/components/rem/editorial-pass.cjs (FULL read -- extract editorial pass prompt with 7 sections, lines ~55-164)
    - modules/reverie/components/session/sublimation-loop.cjs (FULL read -- extract sublimation system prompt, lines ~61-94)
    - modules/reverie/components/context/context-manager.cjs (FULL read -- extract face prompt assembly pattern)
    - .claude/unfuck-the-whole-damn-thing.md lines 1027-1049 (prompt extraction inventory)
  </read_first>
  <action>
Create 7 markdown template files in modules/reverie/prompts/:

**1. framing-full.md** (from referential-framing.cjs full mode):
- Extract the full referential framing string literal.
- Slots: `identity_core` (required), `relational_context` (required), `conditioning_state` (optional).
- Wrap in `<referential_frame>` XML tags as the original does (per Phase 10 decision).

**2. framing-dual.md** (from referential-framing.cjs dual mode):
- Extract dual framing string.
- Slots: `identity_core` (required), `relational_context` (required).

**3. framing-soft.md** (from referential-framing.cjs soft mode):
- Extract soft framing string.
- Slots: `identity_core` (required).

**4. rem-quality-eval.md** (from quality-evaluator.cjs):
- Extract the quality evaluation prompt.
- Slots: `fragment_body` (required), `fragment_frame` (required), `fragment_type` (required), `domain` (required).
- The prompt asks an LLM to evaluate fragment quality.

**5. rem-editorial.md** (from editorial-pass.cjs):
- Extract the editorial pass prompt (the largest -- ~100 lines with 7 sections).
- Slots: `domain` (required), `fragments` (required, type array), `self_model_summary` (optional), `pressure_data` (optional).
- Use {{#each}} for fragment iteration, {{#if}} for optional sections.

**6. sublimation-system.md** (from sublimation-loop.cjs):
- Extract the Tertiary system prompt.
- Slots: `cycle_instructions` (required), possibly `domain_focus` (optional).
- This is the sublimation cycle instruction set.

**7. face-prompt.md** (from context-manager.cjs):
- This is the face prompt assembly template.
- Slots: `identity_core` (required), `behavioral_directives` (optional, default ""), `recall_products` (optional, type array, default []), `referential_frame` (optional, default "").
- Uses {{#if}} for optional sections, {{#each}} for recall products.

CRITICAL: Extract EXACT content from source files. Do not rephrase. Template syntax wraps dynamic parts only.

Each file follows the same JSON frontmatter pattern as Plan 07's templates.
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && ls modules/reverie/prompts/*.md | wc -l && bun -e "const l = require('./lib/linotype/linotype.cjs'); const fs = require('fs'); ['framing-full','framing-dual','framing-soft','rem-quality-eval','rem-editorial','sublimation-system','face-prompt'].forEach(n => { const m = l.parseString(fs.readFileSync('modules/reverie/prompts/'+n+'.md','utf8'),n); console.log(m.name, 'OK'); });"</automated>
  </verify>
  <acceptance_criteria>
    - modules/reverie/prompts/ contains 15 total .md files (8 from Plan 07 + 7 from this task)
    - Each new template has JSON frontmatter with name, version, slots
    - framing-full.md contains <referential_frame> XML tags
    - rem-editorial.md contains {{#each}} for fragment iteration
    - face-prompt.md contains {{#if}} for optional behavioral_directives
    - sublimation-system.md contains cycle instruction content
    - All 7 new files parseable by linotype.parseString without errors
    - Template content matches original string literals (no rewording)
  </acceptance_criteria>
  <done>All 7 remaining prompts extracted to markdown templates. Total: 15 template files in modules/reverie/prompts/.</done>
</task>

<task type="auto">
  <name>Task 2: Update source files to use Linotype templates</name>
  <files>modules/reverie/components/context/referential-framing.cjs, modules/reverie/components/rem/quality-evaluator.cjs, modules/reverie/components/rem/editorial-pass.cjs, modules/reverie/components/session/sublimation-loop.cjs, modules/reverie/components/context/context-manager.cjs</files>
  <read_first>
    - modules/reverie/components/context/referential-framing.cjs (current implementation)
    - modules/reverie/components/rem/quality-evaluator.cjs (current implementation)
    - modules/reverie/components/rem/editorial-pass.cjs (current implementation)
    - modules/reverie/components/session/sublimation-loop.cjs (current implementation)
    - modules/reverie/components/context/context-manager.cjs (current implementation)
    - lib/linotype/linotype.cjs (parseString, cast API)
    - modules/reverie/prompts/ (all template files from Task 1)
  </read_first>
  <action>
Update each source file to load and use Linotype templates. The pattern for each:

```javascript
const linotype = require('../../../../lib/linotype/linotype.cjs');
const fs = require('node:fs');
const path = require('node:path');

const _matrix = linotype.parseString(
  fs.readFileSync(path.join(__dirname, '../../prompts/<template-name>.md'), 'utf8'),
  '<template-name>'
);
```

**referential-framing.cjs:**
- Load 3 matrices: framing-full, framing-dual, framing-soft.
- The `getFramingPrompt(mode, selfModel)` function (or equivalent) now:
  1. Select matrix based on mode ('full', 'dual', 'soft').
  2. Prepare context from selfModel: `{ identity_core: selfModel.getIdentityCore(), relational_context: selfModel.getRelational(), conditioning_state: selfModel.getConditioning() }`.
  3. Return `linotype.cast(matrix, context).content`.
- Remove all string literal prompts. Keep the mode selection logic.

**quality-evaluator.cjs:**
- Load rem-quality-eval matrix.
- The prompt-building function now:
  1. Prepare context: `{ fragment_body, fragment_frame, fragment_type, domain }`.
  2. Return `linotype.cast(_qualityMatrix, context).content`.
- Remove string literal. Keep evaluation logic (scoring, behavioral/LLM split).

**editorial-pass.cjs:**
- Load rem-editorial matrix.
- The prompt-building function now:
  1. Prepare context from domain data, fragments, self model.
  2. Return `linotype.cast(_editorialMatrix, context).content`.
- Remove string literal (the largest prompt -- ~100 lines of template). Keep orchestration logic.

**sublimation-loop.cjs:**
- Load sublimation-system matrix.
- The system prompt getter now returns `linotype.cast(_sublimationMatrix, context).content`.
- Remove string literal. Keep loop logic (cycle management).

**context-manager.cjs:**
- Load face-prompt matrix.
- The `compose()` or `getInjection()` method now:
  1. Prepare context: `{ identity_core, behavioral_directives, recall_products, referential_frame }`.
  2. Cast the face-prompt template.
  3. May also use `linotype.compose()` to combine face prompt with other injection sections.
- Remove string concatenation. Keep context assembly logic (which sections to include, caching).

IMPORTANT: Each file's PUBLIC API must remain unchanged. The consumers call the same functions with the same arguments. Only the internal implementation changes from string literals to Linotype cast.
  </action>
  <verify>
    <automated>cd /Users/tom.kyser/Library/Mobile\ Documents/com~apple~CloudDocs/dev/dynamo && bun test modules/reverie/ --timeout 30000</automated>
  </verify>
  <acceptance_criteria>
    - referential-framing.cjs contains require for linotype and loads 3 framing matrices
    - quality-evaluator.cjs contains require for linotype and loads quality eval matrix
    - editorial-pass.cjs contains require for linotype and loads editorial matrix
    - sublimation-loop.cjs contains require for linotype and loads sublimation matrix
    - context-manager.cjs contains require for linotype and loads face-prompt matrix
    - No string literals > 100 chars remain in any of the 5 updated files
    - All existing Reverie module tests pass (no regression)
    - Public API of each file unchanged (same exports, same function signatures)
  </acceptance_criteria>
  <done>All 5 source files use Linotype templates. Zero string-literal prompts remain in Reverie .cjs files. All module tests pass.</done>
</task>

</tasks>

<verification>
- `bun test modules/reverie/ --timeout 30000` passes (full module test suite)
- `ls modules/reverie/prompts/*.md | wc -l` returns 15
- grep for string literals > 100 chars in modified .cjs files returns 0 matches
- All template files parseable by Linotype
</verification>

<success_criteria>
All 15 Reverie prompts extracted to markdown templates. All source files use Linotype for prompt content. Zero string-literal prompts in .cjs files. Full module test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/17-persistent-runtime-prompt-infrastructure/17-08-SUMMARY.md`
</output>
