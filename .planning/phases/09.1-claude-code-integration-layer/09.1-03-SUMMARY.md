---
phase: 09.1-claude-code-integration-layer
plan: 03
subsystem: integration
tags: [bootstrap, exciter, lithograph, hooks, migration, transcript, wiring]

# Dependency graph
requires:
  - phase: 09.1-claude-code-integration-layer
    plan: 01
    provides: Lithograph provider (createLithograph, setTranscriptPath)
  - phase: 09.1-claude-code-integration-layer
    plan: 02
    provides: Exciter service (createExciter, registerHooks facade)
  - phase: 04-framework
    provides: Armature container, lifecycle, hook registry
  - phase: 08-single-session-personality-injection
    provides: Reverie hook handlers, Context Manager, 8 hook implementations
provides:
  - Bootstrap with 10 services + 3 providers (Lithograph and Exciter added to core.cjs)
  - Reverie hook registration migrated from direct Armature calls to Exciter facade
  - Session-scoped transcript_path wired from hook payload through to Lithograph provider
  - Validated Exciter registerHooks contract with a real consumer (Reverie)
affects: [phase-10, reverie-module, context-sculpting, formation-pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Exciter-mediated hook registration: modules call exciter.registerHooks() instead of direct Armature access"
    - "Session-scoped provider injection: transcript_path flows from hook payload -> handler -> provider"

key-files:
  created: []
  modified:
    - core/core.cjs
    - modules/reverie/reverie.cjs
    - modules/reverie/hooks/hook-handlers.cjs
    - modules/reverie/hooks/__tests__/hook-handlers.test.js

key-decisions:
  - "Lithograph registered with lathe-only dependency (no switchboard) since transcript events route through Commutator, not Lithograph"
  - "Exciter registered with switchboard+lathe deps, integration tag for discoverability"
  - "transcript_path injection placed at top of handleSessionStart, before compact check, so both new sessions and post-compaction sessions receive the path"
  - "lithograph option uses null fallback pattern matching formationPipeline and recallEngine for backward compatibility"

patterns-established:
  - "Exciter-mediated hook registration: exciter.registerHooks(moduleName, handlers) replaces direct createHookRegistry()+register()+wireToSwitchboard()"
  - "Provider injection via handler options: optional provider passed through createHookHandlers options with null guard"

requirements-completed: [PLT-02, PLT-03]

# Metrics
duration: 4min
completed: 2026-03-24
---

# Phase 9.1 Plan 03: Bootstrap Wiring + Reverie Migration Summary

**Lithograph and Exciter registered in core.cjs bootstrap, Reverie hook registration migrated from 10 direct Armature calls to 1 exciter.registerHooks() call, transcript_path wired end-to-end from SessionStart payload to Lithograph provider**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-24T19:03:05Z
- **Completed:** 2026-03-24T19:07:48Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Lithograph registered as 3rd provider and Exciter as 10th service in core.cjs bootstrap with correct deps, tags, and aliases
- Reverie hook registration consolidated from 10 separate createHookRegistry/register/wireToSwitchboard calls into 1 exciter.registerHooks() call
- handleSessionStart now calls lithograph.setTranscriptPath(payload.transcript_path) on every session start with null-safe guard
- 1254 platform tests passing with zero regressions, including 2 new transcript_path injection tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Register Lithograph and Exciter in core.cjs bootstrap** - `a0ae384` (feat)
2. **Task 2: Migrate Reverie hooks to Exciter + wire transcript_path to Lithograph** - `c924dc6` (feat)

## Files Created/Modified
- `core/core.cjs` - Added createExciter and createLithograph requires, registered as 10th service and 3rd provider with correct deps/tags/aliases
- `modules/reverie/reverie.cjs` - Removed createHookRegistry import, added exciter/lithograph resolution, replaced 10-line direct registry with 1 registerHooks call
- `modules/reverie/hooks/hook-handlers.cjs` - Added lithograph option with null fallback, added transcript_path injection at top of handleSessionStart
- `modules/reverie/hooks/__tests__/hook-handlers.test.js` - Added createMockLithograph factory, 2 new tests for transcript_path injection

## Decisions Made
- Lithograph registered with lathe-only dependency since transcript events route through Commutator, not Lithograph directly
- transcript_path injection placed before compact check in handleSessionStart so both new sessions and post-compaction restarts receive the path
- lithograph option uses the same null-fallback pattern as formationPipeline and recallEngine for backward compatibility with tests that do not provide lithograph

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None - all data paths are fully wired.

## Next Phase Readiness
- Phase 9.1 complete: Lithograph provider, Exciter service, and bootstrap wiring all validated
- Reverie migration proves Exciter's registerHooks contract works with a real consumer (D-10 validation gate passed)
- Lithograph receives transcript_path on session start (D-03 session-scoped injection wired end-to-end)
- Platform ready for Phase 10 (Context Sculpting) which will consume Lithograph for transcript-aware operations

## Self-Check: PASSED

- All 4 modified files verified present
- Both task commits verified in git log (a0ae384, c924dc6)

---
*Phase: 09.1-claude-code-integration-layer*
*Completed: 2026-03-24*
