---
phase: 10-operations-and-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dynamo/lib/switchboard/stages.cjs
  - dynamo/lib/switchboard/pretty.cjs
  - dynamo/tests/stages.test.cjs
autonomous: true
requirements: [SWB-01, SWB-03]

must_haves:
  truths:
    - "All 13 diagnostic stage functions exist as independent async exports"
    - "Each stage returns { status, detail, raw } with status being OK, FAIL, WARN, or SKIP"
    - "Pretty formatter transforms stage result arrays into human-readable terminal output"
    - "Stage functions handle errors gracefully and never throw"
  artifacts:
    - path: "dynamo/lib/switchboard/stages.cjs"
      provides: "13 diagnostic stage functions"
      exports: ["stageDocker", "stageNeo4j", "stageGraphitiApi", "stageMcpSession", "stageEnvVars", "stageEnvFile", "stageHookRegistrations", "stageHookFiles", "stageCjsModules", "stageMcpToolCall", "stageSearchRoundtrip", "stageEpisodeWrite", "stageCanaryWriteRead"]
    - path: "dynamo/lib/switchboard/pretty.cjs"
      provides: "Pretty formatter for CLI output"
      exports: ["formatStageResult", "formatHealthReport", "formatDiagnoseReport", "formatSyncReport", "formatInstallReport"]
    - path: "dynamo/tests/stages.test.cjs"
      provides: "Unit tests for all 13 stage functions"
  key_links:
    - from: "dynamo/lib/switchboard/stages.cjs"
      to: "dynamo/lib/core.cjs"
      via: "require for fetchWithTimeout, loadConfig, loadEnv, DYNAMO_DIR"
      pattern: "require.*core\\.cjs"
    - from: "dynamo/lib/switchboard/stages.cjs"
      to: "dynamo/lib/ledger/mcp-client.cjs"
      via: "require for MCPClient in stages 4, 10-13"
      pattern: "require.*mcp-client\\.cjs"
---

<objective>
Build the shared diagnostic stages module (13 stage functions) and pretty formatter that serve as the foundation for health-check, diagnose, and verify-memory commands.

Purpose: Stages.cjs is the single source of truth for all diagnostic logic. Health-check.cjs (6 stages) and diagnose.cjs (13 stages) are thin orchestrators that consume it -- no duplication. Pretty.cjs provides the shared --pretty output formatter for all CLI commands.

Output: Two production modules (stages.cjs, pretty.cjs) and one test file (stages.test.cjs)
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operations-and-cutover/10-CONTEXT.md
@.planning/phases/10-operations-and-cutover/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From dynamo/lib/core.cjs (module.exports):
```javascript
module.exports = {
  DYNAMO_DIR,        // path.join(os.homedir(), '.claude', 'dynamo')
  output,            // output(result, raw, rawValue) - JSON to stdout
  error,             // error(message) - JSON error to stdout
  safeReadFile,      // safeReadFile(filePath) - returns string or null
  loadConfig,        // loadConfig() - returns merged config object
  loadEnv,           // loadEnv(envPath) - returns { key: value } from .env
  detectProject,     // detectProject(cwd) - returns project name string
  fetchWithTimeout,  // async fetchWithTimeout(url, options, timeoutMs) - fetch with AbortSignal
  logError,          // logError(hookName, message) - writes to hook-errors.log with rotation
  healthGuard,       // healthGuard(checkFn) - once-per-session via PPID
  loadPrompt         // loadPrompt(promptName) - returns { system, user } from .md file
};
```

From dynamo/lib/ledger/mcp-client.cjs:
```javascript
// MCPClient class: constructor(options), async callTool(toolName, args), async close()
// parseSSE(text) - parses SSE stream text into array of JSON-RPC messages
module.exports = { MCPClient, parseSSE };
```

From dynamo/lib/ledger/scope.cjs:
```javascript
// SCOPE: { global(), project(name), session(ts), task(desc) } - factory functions
// SCOPE_PATTERN: /^[a-zA-Z0-9_-]+$/
// validateGroupId(groupId) - returns { valid, cleaned, error }
// sanitize(name) - returns cleaned string or 'unknown'
module.exports = { SCOPE, SCOPE_PATTERN, validateGroupId, sanitize };
```

From dynamo/config.json:
```json
{
  "graphiti": { "mcp_url": "http://localhost:8100/mcp", "health_url": "http://localhost:8100/health" },
  "timeouts": { "health": 3000, "mcp": 5000, "curation": 10000, "summarization": 15000 }
}
```

Stage function contract (every stage follows this):
```javascript
// Returns: { status: 'OK'|'FAIL'|'WARN'|'SKIP', detail: string, raw?: string }
async function stageName(options = {}) {
  const { verbose = false, mcpClient = null } = options;
  // ...stage logic...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stages.cjs with all 13 diagnostic stage functions</name>
  <files>dynamo/lib/switchboard/stages.cjs, dynamo/tests/stages.test.cjs</files>
  <read_first>
    - dynamo/lib/core.cjs (for fetchWithTimeout, loadConfig, loadEnv, DYNAMO_DIR, safeReadFile exports)
    - dynamo/lib/ledger/mcp-client.cjs (for MCPClient constructor and callTool signature)
    - dynamo/lib/ledger/scope.cjs (for validateGroupId and SCOPE exports)
    - dynamo/config.json (for graphiti URLs and timeout values)
    - graphiti/health-check.py (port source for stages 1-6)
    - graphiti/diagnose.py (port source for stages 7-13)
    - dynamo/tests/core.test.cjs (for node:test pattern: describe/it/assert structure)
    - claude-config/settings-hooks.json (for hook registration structure used by stageHookRegistrations)
  </read_first>
  <behavior>
    - stageDocker: returns OK when `docker ps --filter name=graphiti` shows both graphiti-neo4j and graphiti-mcp Up; returns FAIL with container-specific message when either is missing
    - stageNeo4j: returns OK when HTTP GET to neo4j bolt port succeeds (or docker exec neo4j query); returns FAIL with connection error
    - stageGraphitiApi: returns OK when fetchWithTimeout to config.graphiti.health_url returns 200; returns FAIL with status code or timeout message
    - stageMcpSession: returns OK when MCPClient can initialize (connect + send initialized notification); returns FAIL with connection error
    - stageEnvVars: returns OK when OPENROUTER_API_KEY is set in process.env; returns WARN if missing (non-fatal); checks NEO4J_PASSWORD presence
    - stageEnvFile: returns OK when .env file exists at DYNAMO_DIR/../graphiti/.env and contains required keys; returns FAIL if missing
    - stageHookRegistrations: returns OK when ~/.claude/settings.json has hooks section with all 5 events pointing to dynamo-hooks.cjs; returns FAIL listing missing events
    - stageHookFiles: returns OK when all referenced hook files exist on disk; returns FAIL listing missing files
    - stageCjsModules: returns OK when all .cjs files in DYNAMO_DIR/lib/ can be required without error; returns FAIL listing modules that throw
    - stageMcpToolCall: returns OK when MCPClient.callTool('search_memory_facts', {query: 'test', group_ids: ['global']}) returns a result; returns FAIL with error
    - stageSearchRoundtrip: returns OK when search returns a result array (empty is OK); returns FAIL on error
    - stageEpisodeWrite: returns OK when add_memory with test content succeeds; returns FAIL with error
    - stageCanaryWriteRead: returns OK when write-then-read with unique canary ID round-trips correctly; returns FAIL if read doesn't find canary
    - All stages catch errors internally and return FAIL with e.message, never throw
  </behavior>
  <action>
    Create `dynamo/lib/switchboard/stages.cjs` with module identity block `// Dynamo > Switchboard > stages.cjs`.

    Require core.cjs for: `{ DYNAMO_DIR, loadConfig, loadEnv, safeReadFile, fetchWithTimeout }`.
    Require mcp-client.cjs for: `{ MCPClient }`.
    Require child_process for: `{ execSync }`.
    Require path, fs, os.

    Define constants:
    - `GRAPHITI_DIR = path.join(os.homedir(), '.claude', 'graphiti')`
    - `COMPOSE_FILE = path.join(GRAPHITI_DIR, 'docker-compose.yml')`
    - `SETTINGS_PATH = path.join(os.homedir(), '.claude', 'settings.json')`

    Implement 13 async functions, each following the contract `async function stageName(options = {})` returning `{ status, detail, raw }`:

    **Stage 1 - stageDocker:** Run `execSync('docker ps --filter name=graphiti --format "{{.Names}} {{.Status}}"', { timeout: 10000, encoding: 'utf8', stdio: ['pipe','pipe','pipe'] })`. Check output lines for `graphiti-neo4j` + `Up` and `graphiti-mcp` + `Up`. Return OK if both found, FAIL with specific container names missing.

    **Stage 2 - stageNeo4j:** Use `fetchWithTimeout('http://localhost:7475', {}, 5000)`. Return OK if response received (neo4j browser accessible), FAIL otherwise. This verifies Neo4j HTTP is responding independently of Docker status.

    **Stage 3 - stageGraphitiApi:** `const config = loadConfig(); const resp = await fetchWithTimeout(config.graphiti.health_url, {}, config.timeouts.health)`. Return OK if `resp.ok`, FAIL with `HTTP ${resp.status}`.

    **Stage 4 - stageMcpSession:** Create `new MCPClient()`, call a lightweight operation (list tools or similar init), then close. Return OK if no error, FAIL with connection details.

    **Stage 5 - stageEnvVars:** Check `process.env.OPENROUTER_API_KEY` (WARN if missing, non-fatal for core operations). Check `process.env.NEO4J_PASSWORD` (FAIL if missing). Return OK if all critical vars present.

    **Stage 6 - stageEnvFile:** Check `fs.existsSync(path.join(GRAPHITI_DIR, '.env'))`. If exists, read and verify it contains `OPENROUTER_API_KEY=`, `NEO4J_PASSWORD=`, `GRAPHITI_MCP_URL=` lines. Return OK if all present, FAIL listing missing keys.

    **Stage 7 - stageHookRegistrations:** Read `SETTINGS_PATH`, parse JSON, verify `settings.hooks` contains entries for all 5 events: `SessionStart, UserPromptSubmit, PostToolUse, PreCompact, Stop`. For each, verify at least one hook command contains `dynamo-hooks.cjs`. Return OK if all 5 found, FAIL listing missing events.

    **Stage 8 - stageHookFiles:** For each hook entry in settings.json, extract the command path and verify the referenced file exists via `fs.existsSync`. Return OK if all exist, FAIL listing missing files.

    **Stage 9 - stageCjsModules:** Recursively find all `.cjs` files in `path.join(DYNAMO_DIR, 'lib')`. For each, try `require(file)`. Return OK with count if all load, FAIL listing files that threw errors.

    **Stage 10 - stageMcpToolCall:** Using MCPClient from options or create new one, call `callTool('search_memory_facts', { query: 'dynamo health check', group_ids: ['global'] })`. Return OK if response received, FAIL with error.

    **Stage 11 - stageSearchRoundtrip:** Call `callTool('search_memory_facts', { query: 'test', group_ids: ['global'] })`. Verify response has a `content` field. Return OK with result count, FAIL with error.

    **Stage 12 - stageEpisodeWrite:** Call `callTool('add_memory', { data: 'Dynamo diagnostic canary ' + new Date().toISOString(), group_id: 'global' })`. Return OK if no error, FAIL with error.

    **Stage 13 - stageCanaryWriteRead:** Generate unique canary ID via `crypto.randomUUID()`. Write episode with canary content, wait 1 second, search for canary text. Return OK if found, WARN if write succeeded but read didn't find it (eventual consistency), FAIL if write failed.

    Export all 13 functions. Also export `STAGE_NAMES` array: `['Docker', 'Neo4j', 'Graphiti API', 'MCP Session', 'Env Vars', '.env File', 'Hook Registrations', 'Hook Files', 'CJS Modules', 'MCP Tool Call', 'Search Round-trip', 'Episode Write', 'Canary Write/Read']` and `HEALTH_STAGES` array `[0,1,2,3,4,12]` (indices of the 6 health-check stages).

    For the test file `dynamo/tests/stages.test.cjs`, create tests that mock external dependencies:
    - Test stageEnvVars with controlled process.env values
    - Test stageEnvFile with a temp directory containing a mock .env
    - Test stageHookRegistrations with a temp settings.json containing valid and invalid hook configs
    - Test stageHookFiles with temp directory containing some but not all referenced files
    - Test stageCjsModules with a temp lib directory containing valid and invalid .cjs files
    - For Docker/HTTP stages (1-4, 10-13), test that they return FAIL gracefully when services are unavailable (they will be in test environment)
    - Verify every stage function returns an object with `status` and `detail` keys
    - Verify no stage function throws (all wrap in try/catch)
  </action>
  <verify>
    <automated>node --test dynamo/tests/stages.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - dynamo/lib/switchboard/stages.cjs exists and contains `module.exports`
    - stages.cjs contains `async function stageDocker`
    - stages.cjs contains `async function stageCanaryWriteRead`
    - stages.cjs exports exactly 13 stage functions plus STAGE_NAMES and HEALTH_STAGES arrays
    - stages.cjs contains `require('../core.cjs')` or `require(path.join(__dirname, '..', 'core.cjs'))`
    - stages.cjs contains `require` of `mcp-client.cjs`
    - stages.cjs first line is `// Dynamo > Switchboard > stages.cjs`
    - dynamo/tests/stages.test.cjs exists and contains `require('node:test')`
    - `node --test dynamo/tests/stages.test.cjs` exits 0
    - Every stage function in stages.cjs has a try/catch block
    - stageDocker contains `docker ps --filter name=graphiti`
    - stageCjsModules contains `require(` within a try block
    - stageHookRegistrations reads from `settings.json`
  </acceptance_criteria>
  <done>All 13 stage functions exported, each returning { status, detail, raw }, all tests pass, no stage throws on error</done>
</task>

<task type="auto">
  <name>Task 2: Implement pretty.cjs shared formatter</name>
  <files>dynamo/lib/switchboard/pretty.cjs</files>
  <read_first>
    - dynamo/lib/switchboard/stages.cjs (just created -- for STAGE_NAMES export and result shape)
    - dynamo/lib/core.cjs (for output/error signature -- pretty.cjs supplements, not replaces, core output)
  </read_first>
  <action>
    Create `dynamo/lib/switchboard/pretty.cjs` with module identity block `// Dynamo > Switchboard > pretty.cjs`.

    This module formats JSON result objects into human-readable terminal output written to stderr (stdout reserved for JSON per GSD convention).

    Implement and export these functions:

    **formatStageResult(stage):** Takes `{ name, status, detail }`. Returns string: `[OK  ]  Docker: Both containers running` format. Pad status to 4 chars. Color-code if terminal supports it (check `process.stderr.isTTY`): OK=green, FAIL=red, WARN=yellow, SKIP=gray. Use ANSI codes directly (no chalk dependency): green=`\x1b[32m`, red=`\x1b[31m`, yellow=`\x1b[33m`, gray=`\x1b[90m`, reset=`\x1b[0m`.

    **formatHealthReport(result):** Takes `{ stages: [{ name, status, detail }...], summary: { passed, total, ok } }`. Writes to stderr:
    ```
    === Dynamo Health Check ===
    Timestamp: {ISO timestamp}

    [OK  ]  Docker: Both containers running
    [OK  ]  Neo4j: Bolt port responsive
    ...

    Result: 5/6 checks passed
    ```

    **formatDiagnoseReport(result):** Same structure but header is `=== Dynamo Diagnostics ===` and shows all 13 stages.

    **formatSyncReport(result):** Takes `{ direction, files_copied, files_deleted, dry_run, conflicts }`. Writes:
    ```
    === Dynamo Sync: repo -> live ===
    Copied: 3 files
    Deleted: 0 files
    ```

    **formatInstallReport(result):** Takes `{ steps: [{ name, status, detail }...] }`. Writes:
    ```
    === Dynamo Install ===
    [OK  ]  Copy files: 14 files deployed
    [OK  ]  Generate config: config.json created
    ...
    ```

    Export: `{ formatStageResult, formatHealthReport, formatDiagnoseReport, formatSyncReport, formatInstallReport }`.
  </action>
  <verify>
    <automated>node -e "const p = require('./dynamo/lib/switchboard/pretty.cjs'); console.log(typeof p.formatStageResult, typeof p.formatHealthReport, typeof p.formatDiagnoseReport, typeof p.formatSyncReport, typeof p.formatInstallReport)"</automated>
  </verify>
  <acceptance_criteria>
    - dynamo/lib/switchboard/pretty.cjs exists
    - pretty.cjs first line is `// Dynamo > Switchboard > pretty.cjs`
    - pretty.cjs contains `module.exports`
    - pretty.cjs exports `formatStageResult` (typeof function)
    - pretty.cjs exports `formatHealthReport` (typeof function)
    - pretty.cjs exports `formatDiagnoseReport` (typeof function)
    - pretty.cjs exports `formatSyncReport` (typeof function)
    - pretty.cjs exports `formatInstallReport` (typeof function)
    - pretty.cjs contains `process.stderr` (writes to stderr, not stdout)
    - pretty.cjs contains `\x1b[` (ANSI color codes)
    - `node -e "require('./dynamo/lib/switchboard/pretty.cjs')"` exits 0
  </acceptance_criteria>
  <done>Pretty formatter loads without error, exports all 5 format functions, uses stderr for output and ANSI colors for terminal display</done>
</task>

</tasks>

<verification>
1. `node -e "const s = require('./dynamo/lib/switchboard/stages.cjs'); console.log(Object.keys(s).length)"` outputs 15 (13 functions + STAGE_NAMES + HEALTH_STAGES)
2. `node -e "const p = require('./dynamo/lib/switchboard/pretty.cjs'); console.log(Object.keys(p).length)"` outputs 5
3. `node --test dynamo/tests/stages.test.cjs` passes
4. Both modules load without requiring Docker or Graphiti to be running
</verification>

<success_criteria>
- stages.cjs exports 13 async stage functions + STAGE_NAMES + HEALTH_STAGES
- Every stage function follows the { status, detail, raw } contract
- Every stage catches errors internally and never throws
- pretty.cjs provides human-readable formatting for all CLI commands
- All stage unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-operations-and-cutover/10-01-SUMMARY.md`
</output>
